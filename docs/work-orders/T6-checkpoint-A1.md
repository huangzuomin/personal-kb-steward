# T6 A1 — implement ONLY persisted source eligibility now

Same integration session. Root has split the broad checkpoint into smaller deliverables. Stop further broad architecture exploration. Current source/seed/T7 interfaces accepted; M2 canonical-display corrections are still running. In THIS turn implement this bounded checkpoint only, test it, hand off. No CLI/initializer/finalizer/config/case-skill changes yet. Other agents are active; preserve their edits. No private/parent vault, credentials, live config, network/model/subagents/commits.

Own NEW core/card_pipeline.py for source eligibility only, NEW tests/test_source_promotion_inputs.py, docs/iteration-evidence/M2-integration/checkpoint-A1.md and report JSON/log. Root additionally grants NARROW ownership of skills/topic-research-compile/renderer.py to persist the already-returned source limitations and topic hints under card_state.analysis, plus regression in tests/test_source_promotion_inputs.py. Do not change source generator semantics or tests owned by source worker.

Fixed design:
`collect_eligible_sources(index, cfg) -> {notes: [...], upstream_hashes: {source_note_rel:sha}, source_note_paths: {original_rel:[source_note_rel]}, issues:[...], rejected:[{source_note,reason}], topic_hints:[...]}`. Exact naming may be documented, but return a compact explicit structure. No disk writes/model calls/rebuild_index. Use captured index and core.reconcile._text to verify actual bytes against captured hashes.

Eligibility:
- Only persisted files under configured knowledge_dirs(cfg)['sources_dir'], metadata.type=source-note and card_state.version=1/type=source-note. Exclude legacy/malformed/partial/error/unknown/non-llm source state; full coverage + analysis_mode=llm required both outer and stored analysis, nonempty usable info_units. manual_review/review_required flags alone do NOT imply unreadable input: actual review/apply retains them; in the final workflow only persisted approved source files arrive here.
- Full original snapshots must exist in the captured index. Declared outer, analysis and unit source hashes must agree and match ORIGINAL bytes. Never refresh old analysis with new index hashes. Reject self/derived-source loops. Canonical safe relative source paths and known paths only.
- Verify each persisted info_unit's exact quote/coordinates against normalized original text (BOM/CRLF raw hash preserved). Reuse core.claims exact locating/compilation machinery; checking a quote hash alone is insufficient. Reject the card if its evidence is invalid, rather than silently calling partial evidence complete. Question units stay questions; do not upgrade kinds or speaker/factual certainty.
- Deduplicate originals by exact path+hash across valid source cards, merge inherited restrictions and speaker info. Conflicting versions cannot win by order. All currently eligible persisted sources are visible, not just latest batch. This helper does NOT concatenate them for one unbounded model call.
- Return generator notes with rel/title/body/metadata/source_text/source_sha256 from ACTUAL originals, plus metadata.upstream_analysis {source_kind, limitations, speakers}. Whitelist inherited metadata. Source-specific limitations MUST persist even when model omits them downstream. Current source renderer saves `limitations` to visible text but NOT card_state.analysis: add analysis.limitations and structured topic_hints now. Combine its explicit limitations with outer quality_flags, not by parsing display prose. Topic hints are proposed research frames, never authoritative claims.
- Return upstream source-card snapshot hashes as well as original notes so later plan binding can pin both. Stable sorted ordering, explicit rejected reasons without source text/credentials in error logs.

Focused tests: actual source executor/render output persisted in synthetic temp source dir -> collect original notes; stale original/source note snapshot refused; forged quote/offset/declared hash refused; legacy/partial/heuristic excluded; newly proposed-but-unwritten source never eligible; manual_review-but-applied source allowed with limitations intact; full source limitations/topic hints reload; cumulative sources and duplicate original handling; no provider/network. Direct test persistence is fixture setup only, not an E2E apply claim.

Run source_promotion_inputs + source_quality_contract/source_traceability/render_fail_closed relevant tests. No full suite during concurrent work. Report paths/counts and STOP this checkpoint. Root will review and resume A2 orchestration separately. Root ledger/workorders remain root-owned.
