# T10 B — public evaluation round fixtures, offline preparation only

Resume adapter session. Astra accepted adapter engineering (46 independent tests). T6 A2 pipeline integration runs in parallel; final end-to-end runner waits its interface. This task is bounded independent preparation, not that runner.

Own NEW tests/public_round_support.py, tests/test_public_round_support.py, and docs/iteration-evidence/T10/checkpoint-B.md plus generated public examples under T10/offline-round/. Do not edit shared generators, core/public_evaluation.py, existing adapter tests or other workers' files. Preserve others' work. No subagents, live model/network calls, installs, credentials, parent/private vault reads, commits or pushes. Only checkout public source and synthetic fixtures. Root owns workorders and ledger.

Implement deterministic mock-response support for ONE representative evaluation round using the CURRENT ACTUAL five generators. Canonical public baseline manifest ONLY: dialogue_user_ai_01 for atomic seed; research_summary_uncited_01 and all 3 project_case_01 family documents for source cards; concept and case from the eligible case-family inputs; topic with explicit question about conditions for the three-stage conversion chain and all three case sources. No private/demo or concept-case alternate fixture substitution. Use exact raw bytes and current quote contracts.

Expected live call schedule is 8: four source calls + one atomic seed call + concept + case + topic. Source/seed use actual executors (read actual interfaces); concept/case/topic actual generators. Offline providers return task-specific valid response JSON, never ready-rendered card mocks. Prove source text sizes/chunk counts fit the configured bound and actual provider count = 8. If current source/seed APIs require more, report precise count before choosing any shortcut; do not omit a card type or crop source text.

Mock responses belong ONLY to the test provider. No manifest expected/prohibited annotations, this mock corpus or rubric ever enter generator input payloads. Public round fixtures should expose source fixture IDs and responses independently so future runner imports test responses ONLY in explicit smoke mode. Production/live path cannot import or use mock answers; preserve physical/module separation. Avoid extracting values from expected annotations to construct prompts. Write tests that capture actual prompts and demonstrate no answer injection.

Demonstrate all five types have traceable compiled outputs; concept definition must use a source-defined concept boundary, case distinguishes observed self-report from mechanism inference, topic uses >=3 cited original sources, labels context differences vs unresolved interpretation correctly, >=2 concrete gaps + >=2 actionable next steps. Seed includes user-vs-AI attribution and a specific tail boundary/next growth point. These are curated MOCK examples, not live quality acceptance. No semantic pass claim by schema alone.

Support functions may create synthetic Note inputs, return provider factories, and generate sample cards/results into explicit test temporary paths. No direct writer marketed as pipeline success, no fake reviewed source-card snapshots, no production plan implementation: actual pipeline runner will reuse T6. Ordinary source input paths should be future isolated-vault paths raw/{fixture_id}.md and quicknote/{dialogue_id}.md, not whole repository paths. Keep useful raw responses and rendered examples with aggregate metrics (types/states/calls/quote hashes), exact commands and limitation statement.

Run only your new focused tests. Stop with compact handoff and paths. Do not start live evaluation or end-to-end runner yet.
