# T6 B1 — shared topic path and accepted update adapter

NOT DISPATCHED. Resume integrator after A2 review; start ONLY when root says typed update helper accepted. This is a bounded subset of T6-checkpoint-B-lifecycle.md: TOPIC + UPDATE HOOK + TRUTHFUL STAGE ENVELOPES ONLY. Cache/processed/review subset lifecycle is subsequent B2. Same ownership/no-private/no-model/no-subagent/no-commit boundaries. No generator prompt/schema redesign. Other workers own text-integrity entry-gate fixes.

Read the parent B lifecycle workorder for target behavior but implement only:
1. Call accepted prepare_typed_updates for every new typed source/concept/case/topic page BEFORE plan binding. Preserve exact structured page.item for concept/case. Atomic seed keeps its own updater. Source adapter must retain structured item as necessary. No old-card adoption. Identity and managed region begin at creation. Changed source regenerates reviewed update with old object identity, base pins, manual preservation; no silent split_existing_pages skip. Surface helper per-page blocked/noop reasons in stage ledger.
2. Topic joins discover_cards/default init/finalize with explicit/proposed question and bounded relevant eligible sources from existing Retriever. Card metadata/limitations/retrieval pins as accepted generator. Full/stub/zero/disabled/error distinct. No question => not_configured/no call. Single-source concept/case valid; topic min3; validate impossible min/max before any model call. Case skill still case-only. May expose cfg.card_pipeline.topic_questions as a list of nonempty strings (small bounded max) and optional hint fallback; document exact implemented keys and tests. Prefer one explicit question for predictable first-run public evaluation. Do not send model expected answers/rubric.
3. Stage status+reason/input/output/model mode for all relevant five stages, including source/seed no_inputs. No pending inputs can disappear behind “all executed”; expose actual calls/outcomes. Generic per-input lifecycle improvements next B2, so state current limitation explicitly. No source/seed semantic reimplementation.

Use actual A2 integration tests as foundation. Add actual saved-plan/review/apply two-step source then topic test on canonical public three-case family, plus source version update with preserved custom metadata/ID, same-title distinct concept/case, manual-edit block. Tests use public_round_support if accepted/available; otherwise minimal local provider shapes, not ready-page mocks. Show no legacy edits/original changes; exact source/upstream pins still checked. Run focused existing A2 tests + new B1 tests + relevant config/plan-object/update-helper checks. Whole suite waits freeze.

Explicit noncompletion: B1 repeat may still call provider before helper noop. B2 must eliminate this; do not claim unchanged-run model efficiency yet. Deliver report to M3-integration/checkpoint-B1.md/json with stable API/config/command seams for evaluation runner. STOP for root review.
