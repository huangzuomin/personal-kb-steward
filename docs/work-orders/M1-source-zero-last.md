# Source zero final narrow correction

Resume same source session. Root independently ran 65 source/zero/integrity/traceability tests: passed. Main explicit zero behavior accepted in principle; one strict-contract hole remains, plus misleading test assertions. Own core/source_analysis.py, tests/test_source_zero_contract.py and source zero evidence note only. Others own typed update helper/integration. Public/synthetic only, no live calls/private data/commits/subagents.

1. Root astra-source-zero-null-before.json reproduces explicit chunk_viable:null with positive candidates accepted as omitted. Workorder required PRESENT flag be exact boolean. Check key presence; null/string/int must be malformed. Omitted positive remains backward compatible. Add null regression; no other prompt/API change.
2. test_failed_plus_zero_chunk_stays_partial uses CFG chunk_chars4000 on short fixture and never actually exercises failed+zero mixture. Use genuinely multiple chunks and assert >=2 calls and explicit-zero limitation plus error range/partial, no complete outcome.
3. test_neg_empty_blocked_with_zero_calls has self.assertEqual(result['created'], [] and result['processed'], 0), which only checks created list. Split clear assertions for created==[] and processed==0.

Preserve before evidence, add after evidence. Run source-zero/quality/traceability/integrity tests and update API note with exact current contract. Stop. Do not touch pipeline or helper.
