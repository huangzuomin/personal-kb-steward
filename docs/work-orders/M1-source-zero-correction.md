# Source final negative-case gate — explicit useful-content zero

NOT DISPATCHED. Resume source worker. Same public-only/no-live/no-private/no-subagents/no-install/no-commit restrictions. Own narrow source_analysis.py, topic-research-compile executor (renderer only if necessary), source quality tests and own evidence. No pipeline/CLI/other generator changes. Preserve other workers.

Root canonical negative fixture probe: neg_empty -> zero calls/zero cards (correct); neg_irrelevant + model empty key_statements/topics -> one card and processed=1 (WRONG). See astra-negative-source-before.json, ignored .execution/astra-negative-source-probe.py. Actual bug: `no_usable = merged is not None and merged['units'] and not any(...)`; empty units makes predicate false and status ok. This lets an empty model analysis count as successfully compiled material. Preserve before evidence.

Implement explicit zero contract without confusing malformed output/failure with valid irrelevance:
- Prompt permits `chunk_viable: false` AND nonempty `reason` with EMPTY key_statements and topics when a read chunk has no independently useful information. Positive response may omit chunk_viable for compatibility IF valid units exist; if present it must be exact boolean. false + nonempty candidates is contradictory -> error; missing/malformed key_statements or absent viability with zero units is not a successful zero. Never synthesize a smooth summary/card out of no usable evidence.
- Only all chunks actually read with valid explicit zero outcomes, no errors, no budget exclusions, can yield source status=zero, complete coverage and explicit zero reasons, with NO source card. Mixed zero chunks + positive verified chunks can yield full source analysis with only real supported units; zero chunk is a read range, not a provider error.
- All invalid quotes, empty malformed output, failed chunk, budget-excluded content stay partial/error/blocked, never valid zero/full. No empty evidenceless source card marked ok. Preserve existing clean-input heuristic-preview policy but keep non-llm and no semantic-success claim; pipeline apply guard remains.
- Keep processed as actual successful compiled source-card count, so explicit zero has processed=0 and pages=[]; expose handled zero separately in `input_outcomes`, not as fake card success. Add per-input results to executor envelope: source rel/source_sha256, outcome=ok|zero|partial|blocked|error, complete flag, exact generated targets, reason and analysis_mode/coverage. Preserve current envelope keys/APIs. Full llm card ok complete=true; valid fully-read zero complete=true with no target; failure/partial/heuristic incomplete for semantic purposes. This is the seam B lifecycle will consume. Don't write any state or mutate source files.

Use actual canonical neg_empty/neg_irrelevant/neg_damaged and repeated-quote fixture. Explicit negative responses through actual executor -> no card; zero units WITHOUT explicit negative reason -> not ok/processed. Tests mixed positive+zero chunks, failed+zero chunk, capped excluded ranges, invalid viability types/contradictory candidates, normal positive old response compatibility, per-input mixed success/failure mapping exact output targets. Existing quote-invalid/source quality tests stay meaningful; adjust fixtures only when old assumptions depended on fabricated/empty outputs being successful.

Root before probe did not request this new chunk_viable contract (it had source_viable); unchanged original response should now fail as malformed/empty, not become a successful zero. New explicit contract test proves legitimate zero separately. Save after evidence separately, exact logs, and compact API note for integrator. STOP for acceptance. No real model calls.
