# T10 C3 — frozen public runner, bounds and artifact confinement

Not dispatched. Start after root accepts C2. Owner remains public_runner_finish; only evaluation script, runner-specific tests/docs and T10/runner-* evidence. Preserve others. No production generators/adapter/private data/native model/network/install/commits. The full test suite waits production freeze; only focused runner tests here.

1. Freeze exact hashes for all production Python/config/router/workflow/prompt/schema/template inputs actually used, not a handpicked incomplete list. Include every canonical public fixture and manifest byte hash. Include runner and smoke support version in smoke evidence without importing mock responses on the live path. Include untracked new production modules. Exclude .execution, output artifacts, caches, private config and secrets. Verify before every round and at end; mutation must produce visible incomplete results, not mixed-version acceptance. Actual prompt/config payload snapshots remain recorded.
2. Keep one attempt per generator stage, no retries/best-of. Count actual attempted calls including errors/timeouts. Positive fixture full round expects eight calls: source x4, atomic x1, concept x1, case x1, topic x1. Three rounds expect 24, with hard 10/round and 30/total ceilings. Report observed split; intake-only honestly expects five and cannot stand in for the baseline. Model valid-zero is retained as data, not replaced with canned output; any baseline unmet expectation remains incomplete/semantic pending.
3. Fresh confined output root with marked synthetic vaults only. Do not accept prepopulated vaults or escaped/symlink/junction target roots for fixture autoapproval. Keep the existing actual plan/save/review/apply writer. All automatic approval explicitly engineering-fixture-only. No writes to original vault/config.
4. Compare original fixture bytes against frozen originals before and after the FULL round. Count only actual successfully applied typed output targets with parsed frontmatter; do not count backups, README or unrelated markdown. Generated U+FFFD blocks success. Persist all failures and executed targets even if a later apply/report step fails.
5. Remove stale hardcoded topic-missing seam narratives now that B1a exists. Describe real stage errors. Scope/engineering outcome/live semantic status must remain separate. Keep semantic_pass null until root content review; no schema-only pass.
6. Focused tests: smoke cannot call native model; live fake-process flow never imports test answer corpus; full 8-call split; intake scope; failed calls counted; bounds reached before extra invocation; source mutation; production/fixture version drift between rounds; artifact reuse/escape; apply failure after writes; generated replacement text; output counts exclude backups. Reuse C2 tests instead of duplicate scaffolding.

Deliver T10/runner-C3.md/json, exact command and fresh artifact path. Stop for root acceptance. Final real-model three rounds wait root-authorized execution after production freeze and available Claude quota; workers never initiate them.
