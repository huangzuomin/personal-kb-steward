# M1 seed — final independent acceptance corrections

Resume the SAME seed session. You own the same seed files/tests/docs only. Preserve other agents. Public/synthetic data only, no parent vault/config/secrets/network/subagents/commit. Keep prior evidence.

Astra independently reproduced a material defect in the current code. Read `.execution/astra-seed-third-run-probe.py` and `docs/iteration-evidence/astra-seed-third-run-probe.json`: create -> same-statement evidence update -> identical repeat CREATES a second card. Do not merely update a test expectation; fix the identity/evidence-state rules.

Required small corrections:
1. Distinguish knowledge-object identity from evidence-version fingerprint. For an already confirmed atomic thought, exact statement/kind + proposed evidence already contained in recorded evidence is NOOP even when the stored first thought_id differs from the new evidence-set digest. Different statement on same excerpt remains distinct. After merging new evidence, both seed_state and card_state must reflect coherent identities/units. Search confirmed identity independent of user-edited title; ambiguous matches remain review. Test create->update->repeat with ACTUAL matching source bytes, roundtrip persisted content, and original object/revision/manual content retained via existing plan/review/apply where practicable.
2. In prepare_seed_updates, current index SHA is NOT an acceptable replacement for an old evidence source SHA. Current `merged_hashes`/retrieval_source_hashes fallback and create seed_state hash source can silently disagree with persisted evidence. Preserve stored verified hashes, verify all retained source snapshots against current index, and fail closed if old evidence is stale rather than rebinding it. Synchronize outer source_hashes, seed_state and card_state.analysis hashes. Test old retained source changed while new source evidence added; no misleading update accepted. Create proposals with source SHA inconsistent with actual index must be rejected at this seam, not persisted as success. Update old tests using placeholder raw text to use the matching actual snapshot.
3. Atomic mode must not adopt or rewrite an old seed lacking the new versioned atomic card_state just because its title matches. Flag occupied/legacy target or make a distinct reviewed new atomic target; no legacy repair. Preserve explicit legacy topic mode behavior separately.
4. `_locate_evidence` currently ignores an incorrect start_line for a unique quote whereas core.claims rejects it. Align with exact locator contract, including third repeated occurrence and wrong unique line hint; reuse existing compiler/locator if practical rather than divergent rules. Ensure both tests execute (current repeated quote test only two occurrences).

Re-run focused seed suites plus relevant contract/traceability tests; report exact results. Generate/reload samples after fixes, update existing M1 seed report.md/report.json. Stop after handoff; no integration modifications. Root reruns independent probe and reviews.
