# M1 seed final review — focused provenance/identity fixes

Same seed ownership and public-only boundaries, preserve other workers. Make targeted edits to current code; no wholesale rewrite or whole-suite run. Finish reports and public samples after these acceptance corrections.

Astra stopped the previous session at this review checkpoint; all edits remain. Independent second probe is docs/iteration-evidence/astra-seed-second-probe.json: normalized text is incorrectly accepted against an original CRLF hash. Continue from files and complete focused fixes, not exploration from scratch. Keep final report concise, with actual failures disclosed.

1. Raw-byte snapshot verification must be EXACT SHA256(source_text.encode('utf-8')). Do not try added BOM or alternate newline variants until a supplied hash matches. The source handoff already preserves BOM/CRLF. If a test used read_text() and normalized original bytes, fix the fixture to read_bytes().decode('utf-8'), not production provenance checks. Test exact BOM/CRLF passes, normalized text paired with old raw-byte hash fails before provider call.
2. `thought_identity` currently hashes transient unit array indices, not evidence identities. Same statement+index0 from a completely different source collides; reordering units changes identity. Hash actual stable source-information identities (source path/hash/normalized quote locator) together with statement/kind, or keep exact claim identity and evidence identity separately. Rerun reorder => same identity; replacing source at same index => not same evidence; adding evidence to a confirmed unchanged thought yields reviewed update, not early skip.
3. Reuse core.claims locator/compilation rather than another partial `_locate_quote`: current locator stops after 2 occurrences and cannot locate a requested third occurrence; a unique quote ignores a wrong line hint. Assertions/inferences should persist actual compiled claims; questions can persist traced Evidence without pretending to be verified factual claims. Test persisted state roundtrip against original snapshots, not only checking hash fields exist.
4. Dialogue attribution must carry the nearest active explicit speaker label across subsequent sentences on the same line and continuation lines, not run `_speaker_of_line` on each already-split sentence independently. Verify supplied units' speaker against original context as well; don't retain caller-forged speaker merely because quote matches. Keep unclear role unknown. The actual public dialogue's final AI proposal and boundary must be included in a normal default complete run: the current first-12 budget excludes later material. Use a sane bounded full-input default (e.g. up to 64 short units / an explicit total context budget), or refuse with partial state; don't silently treat 40 extracted prefix units as all meaningful input. Full claims require complete provided-input accounting.
5. Note-derived invalid snapshots currently become zero_reason because all units were rejected, while supplied units block. Invalid provenance is error/blocked in BOTH paths, even in mixed note batches; no provider call based on an silently reduced set marked full. Model failure, partial extraction and rejected candidates must not result in ok=True/processed=len(notes). No-model previews may remain reviewable but are not successful semantic generation. Explicit thoughts=[] on fully checked input is a valid zero outcome.
6. Seed update matching/target allocation still needs checks against actual integrated pages: same-title distinct ideas across one batch AND later runs must each survive; ambiguous differently worded thoughts should be review proposals, not silently create repeated duplicates. Stable identity lookup cannot be limited to exact title. Do not overwrite card_state/source_hashes during updates with unrelated current index hashes; validate snapshot against generation provenance and preserve original manual body/custom YAML. Updated persisted evidence state must include the same evidence rendered in the new proposal.
7. Whole rendered seed text must not carry model-invented [[wikilinks]] via title/summary/growth/scope/evidence text. Source/related links only to known canonical paths; pending/free prose safely literal. Retain source quote bytes in structured evidence for verification.

Tests: exact-byte mismatch; unit reorder/source substitution; third repeated quote+wrong hint; real dialogue continuation attribution/tail proposal; invalid note snapshot vs valid zero; provider failure processed=0; same-title distinct across batches; unchanged repeated object/revision; preserved manual content. Existing legacy fixture mode changes only where justified, no weakened safety assertions. Include source/seed shared contract regressions in report but do not edit source-owned tests.

Known temp helpers `run_m1_tests.py`/`dbg_m1.py` are not production deliverables; list them for Astra cleanup if tool controls prevent safe cleanup. Do not bypass a denied shell command. Return report.md/report.json and 2 public mocked sample cards with exact structured input/output evidence; pipeline/live-model acceptance remains separate.
