# T8 independent helper — new typed card identity and reviewed replacement

Astra owns design/integration/acceptance. Resume M2 worker, now owning ONLY new core/typed_card_updates.py, tests/test_typed_card_updates.py, docs/iteration-evidence/T8-updates/. Other workers own pipeline/CLI/generators; DO NOT edit those or their tests. No subagents, live providers/network, installs, parent/private vault or live config/credentials reads, commits/pushes. Preserve other workers. Public synthetic data only. Read accepted generator output shapes, plan_objects/update_base, reconcile._text/_patch_header; reuse existing identity/write infrastructure. No direct writes from production helper.

Implement pure planning helper `prepare_typed_updates(index,cfg,pages) -> {pages:[], outcomes:[], issues:[]}` for NEW source-note, concept-page, case-story and topic-page cards. Atomic seed continues its existing updater; leave it untouched. Input page specs have content/rel_path/operation=create/sources/retrieval_source_hashes (original+upstream)/skill AND `item` containing the exact structured generator candidate. A2 adapter already required to retain item. Current concept/case card_state omits definition/mechanism refs: consume these from `page.item`, verify selected bound claims agree with persisted card_state claims and the item's type/sources/hashes, then persist the exact semantic-key ingredients in your new card_update_state. Do NOT guess definition from first claim or parse visible prose. Source may derive identity from source-card structured metadata; topic question is in card_state.analysis.research_question. Helper returns adapted create/update/noop/blocked proposals; plan_objects exclusively assigns object_id/revision later. Do not invent IDs. Per-page outcomes include input target, chosen target if any, outcome and explicit reason.

Identity is conservative, deterministic and versioned. Source: exact original path (one source). Concept: exact source-defined definition statement (from bound claim), concept_name and kind, plus evidence overlap for reuse. Case: exact bound source mechanism or inference statement, card_level and case origin name, plus evidence overlap; project and mechanism cards distinct. Topic: exact explicit research question plus evidence overlap. Inspect CURRENT output fields; if needed field absent, block/manual_review rather than infer from display prose. Evidence overlap uses original path+quote identity (not transient line offsets/array positions or merely common title). Never merge two same-title/different-definition/mechanism objects. Different names that only seem similar get no automatic semantic merge. Do not invent fuzzy title match logic. Retain semantic key separate from current source-version hash. Existing confirmed card must have valid object_id/revision and this helper's versioned update metadata, type, canonical path; arbitrary old typed or legacy cards never adopted. Multiple identity matches => blocked ambiguity. Metadata changed after captured index => blocked.

New create proposals persist top-level `card_update_state` with version, semantic key ingredients/digest and generated-region digest. Wrap generated Markdown BODY ONLY in exactly one unambiguous managed block (`<!-- pks:generated:start -->` ... end); frontmatter remains canonical. Reject marker collisions in model text and malformed/repeated markers. Do not add whole prior cards to model prompts. Pipeline integration will call helper for every new create before plan binding; A2 interim cards without metadata are not eligible for auto-adoption.

For confirmed match, same semantic input/content/evidence is noop (ignore generated timestamps, run IDs and plan object fields); no revision bump or duplicate. Additions to evidence or changed original snapshot produce reviewed updates only. Safely replace the old generated block in full using freshly compiled candidate/card_state, never keep old claims and relabel them under a new source hash. Preserve all text outside that block byte-for-byte and custom YAML/comments; use _patch_header on only owned generator fields (enumerate allowlist), preserve user-edited display title. If old block digest differs or markers malformed, block ambiguous manual edits rather than overwrite. If original path's version changed AND there is non-whitespace manual text outside the block, block with precise manual-review reason (cannot infer whether handwritten facts belong to old version). Custom metadata alone should not block. Always pin update_base from exact captured target; preserve object_id/revision until plan binding. Every proposed original and upstream hash must match actual captured file bytes via _text, not only index.sha; no silent refresh.

Same target with unrelated identity: deterministic identity-suffixed new path, manual-review diagnostic, never overwrite/adopt old cards. Canonical target confinement within correct configured knowledge directory, no path traversal/alias/junction escape. Check actual index/disk collision before proposing. Same batch identical candidates dedupe; distinct same-name candidates receive stable distinct paths. Avoid cache logic, stage ledger, source scanning and CLI integration (other owner). Helper's noop saves writes only; zero provider calls on unchanged inputs will be pipeline cache task.

Tests use actual current generators with deterministic mock responses where feasible, then real bind_plan_objects and persisted read_note to establish confirmed identity (fixture writes are not pipeline E2E). Cover create->apply-like fixture->identical noop; additive evidence reviewed update same object_id/revision after binding; source raw version change fully replaces managed region+card_state without stale evidence; custom YAML/comment and outside manual text preservation; inside edits block; changed-version + outside manual facts block; stale target/original/upstream hash blocks; same title different semantics; project vs mechanism; multiple matches; batch duplicates; source/topic identities; raw bytes unchanged; legacy not adopted; BOM/CRLF preserves outside block; no links to future candidates.

Run only your tests and relevant plan-object helper tests. Return report, API/identity ingredients, limitations and exact commands. STOP for root review, no pipeline wiring.
