# M2 late review — source statements versus inferred roles

NOT DISPATCHED. Resume M2 worker AFTER typed-update helper checkpoint. Own only core/concept_generation.py, core/case_generation.py, their templates, tests and M2 evidence; source integrity worker owns core/evidence_cards.py and generic input gates (DO NOT edit it). Preserve other work. Public/mock only, no external/private/live/installs/subagents/commits.

Root found current generator code accepts an inference-kind compiled claim in source_defined_boundary and reusable_mechanism_claim. T10 mock's repeated quote mapping accidentally exercised this: concept definition becomes an inferred causal mechanism, case source reusable mechanism and mechanism inference are identical. Quote binding alone does not preserve source-versus-model role. Preserve current public API/ref-style contracts; tighten program behavior, no new model fields.

1. source_defined_boundary requires bound fact-kind (attributed source claim, NOT independently verified fact). Inference reference is dropped with explicit issue; not silently relabeled as source text. Existing suggested_interpretation may still cite facts/inferences as supporting context but is clearly inferred.
2. Concept definition may legitimately be a model abstraction. If definition_claim kind=inference, show visible “建议性定义（模型推断）” instead of unlabeled authoritative source definition. Keep bound canonical statement and persist kind through existing claims; template context computes label without changing model schema. Full/stub classification still depends actual sufficient source-defined boundary, never on relabeling inference.
3. Case reusable_mechanism_claim (source-stated mechanism) requires fact-kind. Inference can appear only via mechanism_inference_claim; invalid reuse ref dropped with issue. Actual template heading should clearly say “来源提出的可复用机制”; inference section already separate. Same text accidentally bound to one inference cannot populate both.
4. Result figures under “来源断言” require fact-kind references; reject inference refs or render separately as inference without source-result promotion (prefer rejection with diagnostic). Applicability conditions may be inferred, but render a per-condition visible inference marker computed from its bound claim kind. Don't call every numerical quote verified; retain attribution limitations.

Tests use valid fact-role response baseline then mutate only bound claim kind; assert actual rendered role labels and rejected fields, not only schemas. No API/candidate field additions necessary: derive view labels from accepted claims, keep exact card_state identity. Preserve historical failed T10 example as evidence; T10 worker separately fixes sample selection. Run concept/case and claim/contract tests; report pending live semantics. Stop for acceptance.
