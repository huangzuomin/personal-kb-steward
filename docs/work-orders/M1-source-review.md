# M1 source review findings — recheck current implementation before fixing

Astra inspected the first implementation snapshot during development. Some issues may already be fixed by your final tests; verify actual final code. Same file ownership/privacy/tool boundaries as M1-source. Do not touch seed/topic worker files. These are acceptance criteria, not cosmetic suggestions.

Astra deliberately interrupted the initial session once reviewable code existed; all edits remain. Continue from files, do not restart. Independent reproductions are in docs/iteration-evidence/astra-M1-initial-probes.json (all-provider-failed coverage still full). Do NOT run the entire tests directory or broad unrelated suites; use the listed targeted source tests plus the two source contract cases. Other workers are editing seed/topic and their failures are not your scope. No compound shell cleanup commands or permission bypass; any tool denial remains a denial, work within allowed tools.

1. Coverage describes successfully analyzed ranges, not merely planned chunks. `_llm_chunk_analysis` skipped/failed chunks must contribute explicit excluded/error ranges; all failed calls cannot produce coverage=full via heuristic fallback. Preserve planned read coverage separately if useful. Record analysis errors in result issues and state, do not increment successful processed for failed/partial/unusable output. Test a tail chunk provider failure and a mixed two-note success/failure through actual executor -> plan -> state path.
2. `source_limitations` currently says heuristic/no semantic understanding even for mode=llm. Use correct mode-specific language. Canonical analysis_mode enum must accept only supported values (prefer existing hybrid for fallback or explicitly extend source schema with documented fallback); no unrecognized heuristic-fallback hidden in rendered card.
3. Current model protocol carries no speaker field; `_statement_entry` drops it, classifier conflates dialogue+AI mentions to unknown, and long unknown texts default article merely by length. Add attributable speaker/role per unit, verify role using quoted text/context; preserve explicit unknown when uncertain. Test actual SYNTHETIC dialogue markers with Markdown bold labels; don't test only idealized Q:/A: strings. AI suggestions cannot become user opinions. Unknown long plain text remains unknown without evidence of genre.
4. `_has_citation_markers` treats the word 来源 as proof of a citation: the approved synthetic uncited report literally says 无外部来源/无任何可回溯的原始来源. That must still flag absent references and unverified numbers. Topic hints must be derived from actual model extraction and validated, not always copied title plus generic placeholder. Keep heuristic limitation honest.
5. Reject/drop unverified statements from usable key facts, not `valid_units or unusable_units`. Model summaries can currently state unsupported content even if every quote failed. Invalid quote output must be visibly unusable and cannot masquerade as a summary supported by verified evidence. Validate kind/text/quote fields without stringifying malformed objects. Verify quotes lie inside the supplied chunk, not any unsent part of original text.
6. Full snapshot raw-byte hash must be validated against `source_text.encode('utf-8')`, including BOM/CRLF. Invalid or incomplete snapshot fields must not silently rebind to cleaned body. Validate before provider calls. Do not suppress a source_error if source_text is still present; current fallback reuses the valid text first and can ignore error. Invalid config should surface a bounded error, not recurse into the same invalid config fallback.
7. Persist versioned info_units, source_type/speakers and exact coverage ranges in Markdown-owned fields so later promotion can reload verified evidence, not parse display paraphrases. Include source path/hash on evidence units or an unambiguous parent mapping. M2 receives original snapshots and validated units. Related pending targets remain plain text.
8. Preserve post-provider secret screening on the complete raw response before extracting fields. A mocked provider returning a secret in an unused field must still be blocked. Diagnostics must use safe_error_message throughout; no raw provider exception stored unfiltered.
9. Empty and damaged fixture input should not generate a plausible complete card. Zero/blocked output with explicit reason is legal. Test negative empty/damaged/repeated-quote fixtures; repeated source text must not inflate independent information units.

Run focused regressions, include a rendered public dialogue source and uncited report plus associated payloads. Report exact changed behavior and residual limitations. Source full semantic acceptance still awaits live public-model evaluation; fixtures are not that evidence.

Persistence interface decision: source frontmatter owns `card_state: {version: 1, type: "source-note", info_units: [...], analysis: {...}}`; analysis includes source_type, speakers, coverage ranges/errors and mode, units include source path/hash and exact quote locators. Use existing `_patch_header` after rendering if necessary, keeping base_frontmatter frozen. The returned structured source data and persisted card_state must agree. Later integrator reads this, verifies original hashes/units, and supplies snapshots to generators.

Existing shared regression test ownership: you may update ONLY `test_source_executor_invalid_analysis_fails_closed_before_page` and `test_source_executor_clean_llm_path_writes_compiling_state` in tests/test_card_contracts.py, adapting stubs to new response/snapshot interface while preserving invalid-type rejection and valid clean-state assertions. Other failed seed tests belong to seed worker; report them, do not edit them.
