# M1 seed review — evidence and attribution are blocking

Astra inspected the first module snapshot while implementation was ongoing. Recheck final code; fix unresolved findings within original M1 ownership/privacy/tool boundaries. Do not edit other workers' source/topic/shared files.

Astra deliberately interrupted the initial session at the review checkpoint; all code/tests remain. Continue current files, do not restart. Independent forged-unit reproduction is in docs/iteration-evidence/astra-M1-initial-probes.json. Run only targeted seed tests and related regressions, no whole tests-directory invocation. Do not widen permissions or use compound shell cleanup commands. Finish corrections and report within this resumed task.

1. `information_units` uses the first 8 signal excerpts and the overall path truncates to 12 but calls coverage full. That can entirely omit later user/AI ideas and treat a fixture disclaimer as a thought. Need bounded full-input extraction or explicit partial coverage/excluded units, with model told limits. Unit selection must cover later substantive material in the public dialogue, including the final AI proposal boundary. No early-prefix truncation masquerading as full analysis. A bounded over-budget rejection is preferable to fabricated completeness.
2. source_text plus any syntactically valid hash is not evidence of a matching snapshot. Verify raw-byte SHA256 including BOM/CRLF before model calls. Full text with missing hash cannot be coverage full. For provided `units`, require supplied verified snapshots, check every source/quote/position/hash against original bytes via core.claims or its locator. Currently arbitrary pre-extracted quotes can bypass any source check. Creating a claim_id alone does not verify quote fidelity; persist real compiled evidence with source hash and normalized coordinates.
3. Speaker metadata for an entire note cannot attribute alternating dialogue. Extract contextual speaker/role from the actual full source (including bold Markdown Chinese labels), or retain explicit unknown. Model payload must preserve labels and continuation context. Test public dialogue and verify final card signals retain who proposed what; don't rely solely on fixtures assigning one speaker to the entire note. AI proposal cannot become user's first-person belief.
4. Preserve thought kind/uncertainty. Current is_question flag changes review text but lets a question-support model output a declarative proven assertion. Require structured kind and verify/flag mismatch, preserve unknown/inference rather than pretend fact. Signals should label source assertions as source statements; question tracing is not proof.
5. Malformed responses are not successful zero-output. All candidates dropped for invalid references/content must yield error/blocked and no processed success; distinguish valid thoughts=[] explicitly. Reject invalid unit IDs instead of silently shrinking their evidence set. Validate field types before slicing/coercion; require a substantive growth basis, no empty basis accepted as satisfying contract. Do not silently truncate a 200-character claim or arbitrary statement field to hide malformed outputs.
6. Secret checks must cover exact outbound payload, whole raw response (including unused fields), and safe_error_message for all exceptions. Current `_model_thoughts` interpolates raw exception text. Keep fail-closed behavior with patched providers too.
7. Relation candidates must be actual relevant indexed objects and report actual matching reasons. Verify Retriever result structure (do not assume zip aligns arbitrary report ordering). A suspicion is not confirmed relation; no_match vs candidates_found report should reflect usable candidates clearly. Pending strings may contain embedded wikilinks; neutralize link syntax in pending free text before rendering.
8. Reuse must distinguish two different thoughts from the same exact source excerpt: thought identity needs exact claim/statement AND source-information evidence, not unit set alone. Same-title distinct idea must remain separate; changed wording only -> manual review unless existing object explicitly confirmed. Stable repeats preserve object/revision; manual body/custom metadata survive any reviewed update. No hashing current index to silently rebind provenance.
9. Audit generic CLI caller's output-state contract: it invokes prepare_seed_updates after rendering. Successful items filtered as duplicate/ambiguous/invalid must not leave misleading processed count. Note any shared CLI fix needed for integrator rather than editing others' ownership.

Add discriminating tests for fake supplied units, mismatched hashes, repeated quote occurrence, full public dialogue attribution/tail coverage, model error vs valid zero, secret in unused response field/exception, same excerpt two distinct thoughts and rerun stable identity. Return rendered fixtures and exact reports. Schema/pytest counts alone are not acceptance.

Shared persistence decision: typed source cards will reload `card_state: {version: 1, type: "source-note", info_units: [...], analysis: {...}}`. Your public units adapter may consume info_units but must also receive original snapshots to verify. Seed renderer should persist a versioned card_state with compiled evidence/claims, original thought statement/kind, source-information identities and analysis (rather than body-only evidence). Keep existing seed_state update bookkeeping compatible; do not edit shared base template.

Existing shared test ownership extension: modify ONLY seed tests in tests/test_card_contracts.py (`test_mindseed_invalid_seed_never_reaches_render`, `test_invalid_typed_seed_cannot_advance_processed_index`) to exercise the new atomic path/default or explicitly legacy topic where appropriate, without weakening invalid schema/no processed assertions. You may adapt `test_real_read_note_and_seed_executor_share_filtered_input` in tests/test_priority_fixes_integration.py for explicit topic mode if it is testing legacy clustering; add equivalent new atomic safety test. Source worker owns the two source contract tests in the shared file; preserve their edits.
