# T6 B1b — wire accepted new-card update planner

NOT DISPATCHED until root accepts B1a. Same integrator. T8 helper NOW ACCEPTED: docs/iteration-evidence/astra-T8-helper-acceptance.md. Its public API: prepare_typed_updates(index,cfg,pages)->{pages,outcomes,issues}, pure/no writes/no IDs. See module docstring and T8-updates/report.md. Do not modify helper or semantic generators; report actual seam conflicts to root. Other owners work only evaluation runner and new pure page_review_scope helper. Preserve others. No private inputs/live model/network/install/subagents/commit. CLI <=1700 lines.

Owned: card_pipeline, initializer/finalizer/executor_adapters, narrowly required CLI adapter, focused B1b integration tests/report. Use existing production writer/review/plan_objects.

1. Every NEW typed source/concept/case/topic proposal passes accepted updater BEFORE plan binding and BEFORE split_existing_pages/collision skipping. Source has real info_units card_state; item is optional. Concept/case exact compiled page.item REQUIRED (A2 preserves it), topic persisted research_question/claims. Atomic seed keeps its own updater. No old-card adoption.
2. Managed body and card_update_state start at creation, so later updates have confirmed identity. Same identity + valid overlapping evidence updates existing object; same title different identity is not silently merged. Source version update replaces generated region, synchronizes machine headers, and preserves safe manual metadata/outside text according to accepted helper. Surface blocked/noop reasons distinctly; noop isn't failure. Do not claim no-model repeat until B2.
3. Preserve input_outcomes, stage metadata, all supplied original/upstream/retrieval pins, base hash/revision and exact candidates. Target remapping from helper must be reflected in per-input targets before saving; do not leave old target aliases. IDs/revisions only plan_objects.
4. Actual saved-plan->review->apply: canonical source creation then raw version change then reviewed source update keeps ID, increments revision once, downstream collector accepts new hashes; repeat is write-noop (provider may still run), no duplicate source card. Concept/case same-title distinct candidates remain distinct. Known manual generated-body edit blocks and isn't overwritten; outside metadata preserved when eligible; legacy existing cards unchanged. Source->applied source->topic proposal->review/apply prove actual pipeline, not test direct card writes.

5. Finish the small carried-forward stage contract items: source/seed no-input runs have explicit no_inputs envelopes with zero calls; default atomic seed wording describes atomic thoughts rather than clustering; finalizer wording includes topic when configured. Do not label empty input as model failure or successful processing.

Run focused B1a/A2+new update tests, object-plan/review/apply and helper affected checks. No full suite while other owners edit. Deliver M3-integration/checkpoint-B1b.md/json with API/persistence seams for B2/T10. Stop for root acceptance.
