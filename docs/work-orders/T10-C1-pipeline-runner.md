# T10 C1 — actual public pipeline runner

DISPATCHED. Resume T10 session. Adapter and offline-round-v3 mock examples accepted; use existing work. Root runs live only AFTER code handoff; ZERO live/native-Claude subprocess calls from coding session. Read T10-public-evaluation-harness.md for full constraints. Public canonical fixtures only. No private vault/config/secrets/network/installs/subagents/commits. Others edit integration/helper: preserve changes; do not modify their files.

Own scripts/evaluate_public_baseline.py (new), focused runner tests, docs/public-baseline-evaluation.md and docs/iteration-evidence/T10/runner-*. Leave core/public_evaluation.py and existing adapter behavior unchanged unless an actual isolated blocker is reported; no production generator changes.

Integration sequencing: A2 is accepted; B1a currently adds topic to default discover_cards and cfg.card_pipeline.topic_questions, preserving current API `discover_cards(index,cfg,*,run_id,use_llm=True,kinds=...,providers=...,skill=...,now=...,retriever=...)`; finalizer `make_finalize_plan(...,providers=...)`. B1b will subsequently wire update helper, B2 receipts and B3 subset review later. Your runner uses existing production entrypoints and should work with those additions. Start manifest isolation, actual intake save/review/apply, provider recording, metrics and CLI; if topic path isn't ready at handoff, document exact missing integration seam and stop rather than creating a second topic writer or editing integrator-owned files. Root will resume you for final integration. Do not call direct generators to masquerade as production pipeline.

Round design (fixed for 3 rounds, fresh isolated vault each): canonical 4 raw sources (uncited report+3 case family) + dialogue quicknote. Actual initialization plan invokes 4 source calls +1 atomic seed; save/review/apply through existing steward helpers. Then actual finalize plan invokes concept/case/topic once each with one explicit question matching offline support. Total target8calls/round,24total; hard10/round30total including failures. No retries/best-of/filtering across rounds. Empty/damaged/irrelevant negative checks are already unit coverage; keep this C1 positive schedule simple, do not silently add live calls. A source/card failure means incomplete round, never substitute mocked page or continue claiming success.

Use actual generator prompts/payloads with accepted adapter and manifest-only context. Provider callback registers exact actual payload from public fixture and actual generated intermediate snapshots, then adapter.bind_for_round. Live path must NEVER import expected answer fixtures/tests/public_round_support; explicit smoke may lazily import that test-only mock provider. System/user prompts and return text preserved; fixture rubric and expected/prohibited annotations stay review-only. Fixed nonsecret synthetic config built in code, no user's config.json/.env. Snapshot generator/prompt/schema/code hashes at start and verify unchanged before every round; if active code changes, block/stop rather than mixing versions. Root will run live after full freeze.

Persist output under a fresh explicit artifact root, refuse reuse/collisions/path escapes, no writes outside it except adapter's empty temp cwd. Preserve each round's isolated vault, original byte hashes, actual saved plans/queue/apply evidence, raw results/prompts, output cards, stage states, call counts and failure details. Test-only auto-approval is allowed ONLY inside newly created marked synthetic vault under this artifact root; use the normal review commands. Label this engineering fixture approval, NOT human semantic acceptance. Never approve a user vault. The runner cannot declare semantic pass: metrics separate execution/schema/provenance checks and pending Astra content review.

Expose explicit --smoke and --live mutually exclusive, --output-dir, bounded rounds3/default and adapter settings if needed. Live explicit flag required. Exact commands documented. Source fixture bytes unchanged after execution; all5cardtypes accounted for individually, missing type => failed/incomplete round. Unexpected provider failure/raw malformed output retained. No silent JSON repair.

Tests: offline smoke runs actual plan/save/review/apply and no native CLI called; mocked adapter subprocess contract unchanged; live import guard proves no test answers loaded; output confinement/refuse existing root; fixed version change detection; budget and incomplete round never semantic success. Run focused tests only, not full suite while integration active. Handoff stable invocation, report paths, remaining blockers; STOP. Root executes live later.
