# M2 independent acceptance review — evidence-bearing sections

Resume SAME M2 generator session after first delivery. Same file ownership/security/data constraints. No pipeline/config/CLI edits. Keep failed evidence. Implement small coherent corrections, not a new generic framework.

Astra inspected the first draft. After your first tests pass, independently verify these cases before declaring handoff:

1. Concept definition must itself be an evidence-bound claim, not any free string accepted merely because SOME unrelated judgment compiled. Require an explicit exact compiled claim reference for definition or use that compiled judgment statement as definition. Source-defined boundaries whose evidence references are rejected must be dropped with diagnostic, NOT retained as unsupported interpretation. Suggested interpretation requires valid supporting compiled context and explicit inference label; a freeform assertion that evidence was missing isn't supporting context. Near-concept/applicability boundary is a minimum concept requirement: absence => honestly limited/manual review rather than full. Machine binding proves traceability only; semantic support remains Astra/human review.
2. Case outcome prose/figures and mechanism/applicability narrative cannot outlive rejected evidence as valid content. Bind material outcome statements to compiled claims, drop unsupported items and retain diagnostic. A separate free result.summary must not keep a rejected invented number/outcome. Inferences clearly labeled and grounded; record context/action/result/mechanism/applicability sections genuinely present for full cases. DO NOT require numbers: qualitative observed outcomes with valid evidence can form a full case. A numeric quota would pressure the model to invent metrics. Source assertion means not independently verified, not automatically self-reported by a specific person; retain actual provenance rather than overwriting every source's provenance classification.
3. Input source-card quality limitations/speaker information must survive the adapter seam. Allow explicit compact upstream analysis metadata in note.metadata (original snapshot remains evidence), include material limitations in provider payload and persist/render program-provided limitations even if model omits them. Never let a model erase source-specific limitations. Bound and screen this metadata as part of full context; don't transmit arbitrary whole-index metadata.
4. Exact candidate dedupe must UNION evidence of identical claims from different inputs, recompute usable source set/hashes, and preserve distinct mechanism/project content. Current _dedupe_* only appends previously unseen claims and silently discards new evidence attached to an existing identical judgment. Test duplicate concept/case candidates from complementary sources, persistence reload+validate_claims and all citations/source hashes retained. Don't claim differing card_level alone proves materially distinct content: if exact same story and mechanism under both levels, flag ambiguous duplicate rather than automatic full second card; a focused mechanism card with distinct supported abstraction is valid.
5. Verify output-contract field shapes equal parser expectations (evidence_claim/claim refs currently string in fixture output contract versus resolve_claim_refs list). No silent drift between real prompt and tests. Fixtures read_bytes().decode('utf-8'), not read_text() with false 'keeps CRLF' comment; include raw BOM/CRLF proof. Inspect actual rendered multi-item lists for Jinja trim_blocks newline concatenation (topic had this bug); persisted state and schema validation plus actual readable content both required.

Add focused regression probes for these cases; run own module suites plus claims/contracts/source traceability. Update report.md/report.json with exact states and public mock samples. Root will independently test and review actual rendered cards; do not claim live semantic acceptance.

Independent root reproduction now saved: docs/iteration-evidence/astra-M2-initial-probe.json and .execution/astra-M2-initial-probe.py. It shows state=full, invented definition retained and rejected boundary retained. Prior worker process stopped at this review checkpoint with all edits preserved; exit -1 is intentional, not completed handoff. Other seed owner is editing concurrently; run your relevant focused modules/claims/contracts/source traceability only, do not keep rerunning full suite while unrelated modules change. Report shared failures without editing other's modules.

