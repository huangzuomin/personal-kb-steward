# T6 A1 review corrections — verify persisted snapshot and malformed state

Same A1 ownership/restrictions; do NOT start A2 yet. Root reproduced four failures using your real producer fixtures: `.execution/astra-T6-A1-probes.py` and `docs/iteration-evidence/astra-T6-A1-probes-before.json`.

1. Mutate a persisted SOURCE CARD after build_index: collector still returns eligible=1. `_text` is only called on original, never source card. Verify BOTH captured snapshots before using metadata; reject changed/deleted/undecodable source card with a per-card reason. Original deletion/read failure likewise a per-card rejection, not an uncaught exception. Remove redundant unguarded second original read_bytes; `_text` already verifies bytes/hash.
2. Remove all four unit coordinates: collector still accepts it. For this typed persisted contract require each start/end/start_line/end_line to be present and exact int (bool/float/string invalid), with valid ranges, then compare to located values. Do not silently reconstruct missing metadata as if stored evidence were fully valid. Version must also be exact int 1.
3. Malformed analysis.source_hashes=['not-a-map'] crashes AttributeError; kind list and malformed list fields can also crash/turn strings into character arrays. Validate shapes of hash maps/units/limitations/speakers/topic_hints/quality flags as needed BEFORE access; reject malformed card with reason while continuing other valid cards. Don't catch programming exceptions broadly and silently. Test mixed valid+bad cards.
4. Duplicate original with first source_kind=None then second='unknown' crashes min(None,str). Resolve accumulated known kind values at finalization: none/unknown alone remain unknown; one known kind plus unknown preserves known kind with explicit provenance; genuinely conflicting known kinds => unknown + visible limitation/issue listing alternatives, NEVER pick lexicographic minimum as semantic truth. Test reversed order and three cards.
5. Preserve the actual source-card origin for each topic hint; currently every merged hint is tagged cards[0], including a hint only in a later source card. Return one entry per actual origin (or a documented sources list) instead of misattribution. Keep stable order.
6. Correct docs: unit start/end are normalized Unicode character offsets, not byte offsets; SHA covers original bytes.

Add focused regressions and rerun A1+source renderer/traceability quality tests. Root probe output before must be preserved; write after to a different file. No broad suite. Update report then stop; A2 starts after this checkpoint is accepted.
