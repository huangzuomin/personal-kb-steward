# T10 public evaluation harness — actual model, actual generators

Owner Claude verification coding executor, Astra review/operator. NOT dispatched yet. Implement only after all accepted generators expose their final APIs. No live calls from the coding-agent session. Public/synthetic fixtures only; no parent/private vault, .env/config.json, credentials, subagents, install, commit or push. Preserve other work.

Own scripts/evaluate_public_baseline.py (new), tests/test_public_evaluation_harness.py, docs/public-baseline-evaluation.md and docs/iteration-evidence/T10/*. Keep outputs large files out of prompts; report paths and compact metrics.

Create a harness with two separate commands/modes: deterministic offline smoke (patched provider), and root-invoked explicit live public evaluation through the already-authorized Claude CLI. This is a test provider adapter, not a production-provider change. Do not alter core/llm.py or read live credentials. Never launch nested Claude while this coding session is running; root executes live mode after handoff.

Live adapter accepts only manifest-listed SYNTHETIC fixture data and system/user payloads produced by the actual source/seed/concept/case/topic generators. Never feed golden cards, fixture expected answers/rubric, private content or whole index as model input. Exact system prompt + JSON payload must be logged as public evidence and submitted unchanged in semantics. CLI has no file/network/tool permission for evaluation responses (empty tools / strict MCP config, no permission bypass), output JSON preserved. Use installed configured model; do not buy new API access. No external API fallback.

Run ID/versioned output directory, fixed requested model/settings if available, source byte hashes, prompt hashes, full raw returned text, parsed payload, rendered cards, call count/time/usage, failure details and stage outcome. Parse only exact model result content; never repair an invalid answer invisibly. Failed attempts remain evidence. No automatic retries by default. Explicit bounds: <=30 live completion calls total for 3 rounds, <=10 per round; refuse before exceeding budget. Each round must cover five card types on 5 base public positives (dialogue, uncited report, 3 related cases). Sources may be processed one per type-required input; reuse within same round only, not across rounds. Call-count design must be shown before execution; do not omit a type to make budget fit.

All 3 rounds use the same fixed public fixture source versions and prompt/generator version. Model text variation allowed; hard gates fail on invented source/hash/link, unmatched quote, wrong state/metadata, failure mislabeled success, damaged Chinese. Content is independently reviewed by Astra against docs/demo-baseline-rubric.md; harness may collect facts and machine checks but cannot declare semantics pass by schema alone. 3 valid rounds cannot prove private-demo equality; label public semantic evidence explicitly.

Use temporary isolated vaults with existing test harness conventions, independent config and review/run/state paths. Actual generator -> plan -> review -> apply path should be exercised via accepted pipeline integration harness; reuse it instead of a second writer. Preserve originals/source hashes. Output paths beneath requested artifact dir; reject external targets. No real original-vault mutation or legacy cleanup.

Offline tests mock subprocess and response shapes, exercise invocation bounds/timeout/failure, verify prompts contain only allowlisted fixture data, prove no CLI/provider call in smoke mode, logs record failed outputs, and all required card types are reached. Report precise invocation root should run, limitations, command exits. Deliver implementation; root separately runs live calls and accepts content.

Installed CLI help verified by Astra: `--tools ""` disables all built-in tools; `--strict-mcp-config` skips ambient MCP; `--system-prompt` accepts the generator system prompt; `--output-format json` returns one result envelope; `--no-session-persistence` is available. Do not use `--bare`: installed 2.1.278 documents that bare ignores OAuth and requires API-key auth, which would break the already-authorized subscription path. Keep normal authenticated invocation with tools disabled; no key fallback.

Native executable verified through npm shim: C:/Users/zooma/AppData/Roaming/npm/node_modules/@anthropic-ai/claude-code/bin/claude.exe. Use native argv with shell=False; payload via stdin; model cwd an empty temp outside vault ancestry; disable slash commands as well as tools/MCP. Separate provider utility checkpoint is T10-checkpoint-A.md.
