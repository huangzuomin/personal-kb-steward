# T8 update helper root review — not accepted

NOT DISPATCHED. Resume M2 worker after M2 role correction handoff. Own typed_card_updates.py and its tests/reports only. No source/seed/pipeline/generator edits, no private/network/live/subagents/installs/commits. Root reproduced SIX failures in astra-T8-probes-before.json; ignored .execution/astra-T8-probes.py. Preserve before, rerun to different after path. Current 25 tests are insufficient and source/case constructed test shapes are WRONG. Implement corrections, not a parallel helper.

## Actual contracts first

1. ACTUAL case generator candidate uses `reusable_mechanism` + `reusable_mechanism_claim_ids`, `mechanism_inference` + `mechanism_inference_claim_ids`. `_claim` fields belong to model response, NOT compiled candidate. Resolve the actual IDs against persisted compiled claims and ensure exact statement/kind/role consistency; accept actual generate_cases output. Source card_state has info_units + analysis, NOT claims. Accept actual rendered source card and verify its units against raw snapshot (sa.locate_quote + exact coords/kind/hash). Source may derive identity/provenance from its persisted structured card_state/frontmatter without item (original brief explicitly permitted this); concept/case require exact item, topic has persisted question and claims. Do not fabricate claims or reverse-parse prose to satisfy helper. Test ACTUAL source/case/topic generator+renderer outputs, not handmade surrogate schemas.

## Coherent reviewed updates

2. Existing update replaces card_state but leaves outer source_hashes/sources/coverage/status/etc OLD. Root appended source bytes, got update with old outer hash and new internal hash. Expand explicit owned-field allowlist to synchronize machine-owned sources/source_hashes, related, status/stage/confidence/review_required=True, coverage/analysis_mode/schema_version/generator_version/quality_flags/origin and card_state/update_state (only present/new applicable fields). Preserve created, manual display title, custom YAML/comments and outside text. Document tag policy; retain user tags if safe. Reuse reconcile._patch_header rather than bespoke single-line YAML replacement which mishandles multiline values. Helpers may locally validate exact allowed keys then call existing patcher. Keep original object_id/revision until plan binding. Test full reload outer/inner/source-unit/claim consistency and downstream collector eligibility after source update.
3. Return `dict(page, ...)` preserving item, origin, analysis_mode, confidence and review metadata, with changed target/content/hash/base fields applied consistently. Current helper strips item/review_required/confidence/origin. Every create/update remains explicitly review_required=True. No stale target alias; top-level rel_path/target/canonical values agree as applicable. Existing review/apply gate must still reject unreviewed candidate.

## Snapshot/identity fidelity

4. `_exact_bytes` currently just reads bytes; it DOES NOT enforce the captured index. A source changed after index with proposal's new hash is wrongly accepted. Require an actual indexed Note for each dependency, canonical path with no alias/escape, _text(note) verification and expected==note.sha256. Catch expected read/decoding conflicts per page. No silent rebasing. Validate hash VALUES across item/frontmatter/card_state/every claim or source unit and retrieval pins, not merely matching key sets; root forged item.source_hashes=zero was accepted. Use actual compiled claim validation against captured documents; reject missing/inconsistent evidence, source state type/version and non-exact bool versions.
5. Promised evidence overlap is never implemented: current matches use only semantic digest. Root same exact definition/name but all evidence moved to a different path → silently update old object, replacing disjoint evidence. For concept/case/topic require verified overlapping (source path, exact quote) evidence for automatic reuse. Source identity is path and must allow genuine version replacement. Same semantic key with disjoint evidence => explicit manual-review ambiguity or distinct reviewed create, NEVER silent reuse. Include bound claim kind/role in semantic ingredients (fact vs inference must not collapse). Validate stored semantic_digest against stored ingredients/version and existing card_state before trusting it; canonical matched target must remain under correct configured type directory.
6. `version_changed` currently treats a newly ADDED source as changed old version, blocking additive updates if outside manual text exists. Only common old/new source paths whose hashes differ are version changes. Add source + keep existing source versions + outside hand text must allow reviewed update preserving outside bytes. True same-path changed version + manual outside remains blocked. Source removal must not silently retain obsolete source headers. Tests must cover both.

## Additional safe handling and tests

Handle expected OSError/Unicode/conflicts per page, valid sibling still processed; don't hide programming errors with blanket except. Retain malformed marker/inside-edit and old-card no-adoption tests. Batch dedupe must not erase different source versions; collisions remain stable/reviewed. Successful noop is an outcome, not a failure-quality issue to be mistaken for blocked by integrator. Prove all six root probes fixed plus added-source/manual text and multi-line custom YAML. Valid header/card_state untouched by manual edits must preserve identity/revision across create->bind->apply-like->update->identical repeat. No production write authority added.

Run focused helper + object plan tests; root will independently rerun probes/tests before B1 wiring. Correct report limitations and supersede erroneous prior claims explicitly. Stop for review.
