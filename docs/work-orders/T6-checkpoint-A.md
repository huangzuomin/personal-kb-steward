# T6 checkpoint A — persisted sources and one generator pipeline

Owner Claude integration agent. Astra designs and accepts. Execute this checkpoint only; full next phases are in M2-M3-integration.md. Other agents share the checkout; preserve their changes. Use only public/synthetic fixtures, no private/parent vault/live config/.env/credentials/model calls/network/subagents/install/commit/push. Python environment already supplied.

Scheduling: source, atomic seed and topic generator engineering are accepted. Concept/case owner is completing scoped evidence-binding corrections, but their public call signatures and result envelope are fixed (generate_concepts/generate_cases(notes,cfg,*,known_paths,analysis_mode,call_provider,now) -> state/reason/items/pages/claims/analysis/issues). Start independent source eligibility and orchestration seam work now; do not edit those generators, and do not claim integration acceptance until their final handoff and your actual-generator tests pass. Read docs/card-contracts.md and their acceptance/report API sections, existing plan_objects/review/apply flow, and M2-M3-integration.md for invariant context. Do not reimplement generators.

Own core/card_pipeline.py (new), core/initializer.py, core/finalizer.py, core/executor_adapters.py (optional), core/skill_executor.py, necessary core/card_contracts.py registration, core/config.py, scripts/validate_config.py, config.example.json, minimal CLI adapters (<=1700 lines), case skill executor/schema and pipeline integration tests. No source/seed/topic/concept/case generator edits; report a precise needed change to Astra if blocked by them.

Deliver A:
- Read eligible persisted version-1 source card_state from configured sources directory; use existing index captured original bytes; verify coverage full, nonempty usable units, original hash equality and unit quotes/locators. Reject missing/stale/malformed/partial/heuristic-only evidence for baseline-qualified automatic promotion. Keep source-specific limitations/speaker attribution in downstream context. Do not parse display paraphrases as raw evidence. Do not use legacy cards or newly planned unapproved source pages.
- Thin adapter calls accepted generate_concepts/generate_cases once each with bounded full snapshots, known existing paths, injectable provider. Shared default promotion path called from actual initialize/finalize, and case skill. Explicit legacy configs may retain prior behavior, but old aggregation output must not claim new baseline quality. No blanket two/three-source floor for concept/case. Without LLM, record blocked/not_configured or limited preview truthfully.
- Planned pages follow existing bind -> saved plan -> review -> apply, no second writer. Pin both upstream source-card snapshot hashes and original hashes. Only link existing objects; planned sibling paths remain pending. Unknown paths/missing hashes/error output cannot reach apply as valid.
- Tests actual isolated workflow: first init compiles sources -> review/apply -> subsequent init/finalize discovers concept/case from cumulative eligible sources -> review/apply -> reload claims and validate original source hashes. Single-source concept/case legitimate; unrelated/explicit zero legitimate; provider error not processed success; source change blocks stale promotion. Case skill delegates same generator. Reviewer rejects one candidate; approved siblings have no dangling links. A model mock at provider boundary is allowed, not replacing generator with ready pages.
- Keep no-op/update extension points explicit; final cross-batch idempotence and ledger implementation are NEXT checkpoint, not claims this checkpoint has achieved.

Evidence docs/iteration-evidence/M2-integration/checkpoint-A.md + report JSON + logs/artifacts. Record exact commands/counts and actual invocation path. Notify completion; do not start B without Astra review.

Read Astra source/seed/topic acceptance reports for verified state. Seed worker report's claim that CLI omits vault_index/retriever is stale: current CLI passes both; do not reimplement. Preserve generator ownership. Do not rerun entire test suite during concurrent M2 edits; focused integration tests only, report genuinely transient dependency failures. Source-only eligibility and source plan/apply tests can proceed independently. All other root ledger/work-order files are root-owned, do not edit them.
