# T7 final review — small correctness patch, no rewrite

Same owned files/privacy boundaries. Preserve completed implementation; make targeted edits and discriminating tests, then refresh all evidence samples (including stub) with current code. No full-suite run. Follow these product decisions exactly.

1. Shared origin is not a blanket ban on a full topic. Three distinct records about one bookstore (case, later measurement review, failure in a different setting) are the approved public full-topic family; dependence must be disclosed, confidence limited, and never counted as independent corroboration. Actual byte-identical copies count as one source for sufficiency; explicitly identical original-source identities also collapse for source count. A model's nonempty shared_provenance note alone must NOT force every 3-source family to stub. Test the actual case-family payload with a shared-background note -> full with limitation; three copies -> insufficient stub.
2. Zero valid compiled judgments means error/no substantive page, not a stub preserving an unsupported freeform summary. Reserve stub for genuinely supported content with insufficient scope/source count. Distinguish missing/invalid topic_viable from explicitly false valid zero. No malformed response may be silently classed as legitimate irrelevance.
3. Duplicate exact judgments currently compile independently and create duplicate claim_ids, so persisted state can fail core.claims.read_claims. Deduplicate exact claims and merge distinct validated evidence, or refuse duplicate response with a precise issue; whatever is emitted MUST reload via validate_claims. Test duplicate statement+kind with complementary source evidence.
4. Conflict evidence relationship is relative to EACH claim. Two opposing statements can each have supports evidence; requiring a global set {supports,contradicts} across unrelated claims is logically wrong. Same-source dialogue can document two sides; >=2 files is not a valid requirement for a recorded disagreement. Use explicit two side references to distinct compiled claims + stated point of disagreement; retain model-proposed/unverified semantic status (program checks evidence presence, not truth). Do not call two agreeing claims a machine-verified contradiction. Tests: agreeing pair does not acquire verified conflict merely from relation labels; opposing recorded statements in same source can be represented as documented disagreement without pretending fact adjudication. No forced conflict quota.
5. Canonical path checks reject raw//x.md and raw/./x.md too. Handle duplicate rel snapshots consistently (different hash for same path => error). Final full read coverage cannot hide a refused input; retaining partial while a supported narrower topic is produced is allowed, but scope limitation must be visible.
6. All rendered evidence claims/analysis must roundtrip, freeform links remain safe, and final sample stub must be regenerated (the old artifact still has partial coverage solely from two sources and no card_state). Samples are mock fixtures only. Keep source-map labels and quality sections plain Chinese, avoiding instructional phrases aimed at the model.

Report precise signatures/state meanings and any integration registration needs. Run topic tests and claims test only for this patch; branch-wide seed contract failures are assigned separately.
