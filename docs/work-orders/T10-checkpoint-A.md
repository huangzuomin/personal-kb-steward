# T10 checkpoint A — bounded public Claude provider adapter

Owner Claude evaluation-harness coding agent, Astra operator/reviewer. This independent adapter may start while M1/M2 finish. Do NOT implement live quality judgments, invoke Claude inside this coding session, modify generators or depend on transient APIs. Preserve others' edits. Public/synthetic fixture files only; no private/parent vault, .env/config.json, credentials, installs, subagents, commits/push.

Own NEW core/public_evaluation.py (test-only provider utilities), tests/test_public_evaluation_harness.py and docs/public-baseline-evaluation.md. Next checkpoint will wire scripts/evaluate_public_baseline.py to accepted generators/pipeline. Read T10-public-evaluation-harness.md for final purpose, but implement this bounded checkpoint only.

- Callable adapter `(cfg, system_prompt, payload)->str`, explicit live enable, fixed model/effort, global max_calls <=30 and per-round <=10. Budget counts every subprocess attempt including failures/timeouts and refuses BEFORE excess. Root enables live after coding exits. Offline unit tests patch subprocess and assert no real CLI/network used.
- Native CLI available at C:/Users/zooma/AppData/Roaming/npm/node_modules/@anthropic-ai/claude-code/bin/claude.exe (confirmed by npm shim). Prefer caller-supplied/native executable argv list, shell=False. Do not pass content through cmd or string-built PowerShell. Pass JSON user payload via stdin, exact generator system prompt via --system-prompt. Retain authenticated OAuth path; DO NOT use --bare. Set --tools "", --strict-mcp-config, --disable-slash-commands, --no-session-persistence, --output-format json; optional --setting-sources "" only if installed help confirms accepted. Explicit configured model for evaluation is claude-opus-5[1m], effort medium unless root overrides.
- Run native model process in a new empty temp directory OUTSIDE checkout/private vault ancestry so repository prompts/AGENTS/CLAUDE cannot get auto-loaded. No resume/session inheritance. No credential access or auth changes by harness. Exact invocation settings logged sans credentials. No invisible retry/fallback.
- Immutable allowlisted synthetic fixture snapshots loaded ONLY from tests/fixtures/card-baseline/manifest.json; resolve paths under that directory, require SYNTHETIC provenance and hashes. Do not feed expected answers/rubric/manifest annotations as model content. API adapter itself should accept only a run-created public context bundle, not arbitrary external paths. Validate generator document paths/text/hash against exact fixture snapshots or registered PUBLIC-generated source/seed intermediate state; input structures differ across source/seed/topic, so expose an explicit bounded public-payload registration mechanism with exact hashes (cannot imply a regex scanner proves provenance). Document trust boundary: root harness constructs payload from allowlist; no arbitrary --input option.
- Append per-attempt JSON evidence with run/round/index/model/effort/prompt hash/source hashes/duration/usage/return code/timeout/parsed result/failure. Save exact system/user prompt, raw stdout/stderr/result text under run output dir; preserve errors and do not silently repair malformed answers. Result parsing returns exact CLI result string; generator owns JSON interpretation. is_error, nonzero, missing result and timeout become explicit adapter errors. Bound output directories to requested artifact root; refuse overwrites of existing run.
- Tests budget at/beyond boundaries, timeout/failure accounting, missing result/is_error, argv shell safety and tools disabled, exact prompts, no root/private path reads, corrupted or nonmanifest fixture rejected, recorded failures.

Deliver report docs/iteration-evidence/T10/checkpoint-A.md and test logs. Stop at this checkpoint; root will review then request generator/pipeline integration. No live calls.
