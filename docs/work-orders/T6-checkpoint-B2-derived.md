# B2-derived checkpoint — stable cumulative concept/case/topic discovery

NOT DISPATCHED. Start after root accepts B2-seed. Owner: topic_integration_finish. Same public-only isolated checkout, preserve others, no live/private/config/env/network/commits/B3 edits. CLI <=1700. Own core/card_pipeline.py, pipeline_history.py, initializer.py/finalizer.py receipt transport, narrow existing Retriever scoped-index reuse, focused tests and M3-integration/checkpoint-B2-derived.md/json. Do not alter accepted semantic generator/updater policies to make caching pass.

Goal: actual init/finalize derived discovery performs zero provider calls and proposes no writes when its exact previously evaluated inputs/context/contract and verified outputs remain unchanged. New cumulative eligible inputs still trigger evaluation. Source/seed accepted receipt lifecycle must remain intact.

1. Before each actual generator call, fingerprint the complete evaluated closure: selected originals, their exact upstream source-card snapshots, bounded retrieved context actually supplied (including uncited supplied dependencies), type/question/config/model and real code/prompt/schema/template bytes. No run/time/random IDs/credentials. The same originals A and expanded A+B are different stage closures; never derive cumulative completion from independent A-only and B-only receipts.
2. Integrate receipts through actual saved-plan binding (both init and finalize), not a separate state writer. Explicit valid zero can be reused; pre-call insufficient/no-input/disabled and provider error/partial/block remain distinct. Keep original plan refs and real per-stage provider counts for pending/rejected/unchanged states. Pending/rejected never auto-resubmit unchanged proposals.
3. Solve self-invalidation before bounded retrieval: candidate prior receipt identifies its exact verified derived output cohort (concept/case/topic peers from the same discovery). Exclude those exact current path/object/revision/hash matches BEFORE Retriever limit/top-k, so prior seed/external related context remains available. Do not exclude genuine source/seed inputs or unrelated external cards merely because they were created in the same overall plan. Any edited or independently changed context remains a new dependency. Reuse existing scoped Retriever and preserve full context hashes; no second ranking algorithm and no dropping supplied hashes to manufacture a cache hit.
4. Preserve safe-update base identity/hash separately from generation-context exclusion. A current card with manual generated-region edits must not be silently accepted as unchanged. Confirmed updater NOOP records capture exact verified existing-target snapshots, including the B2-source save-boundary race protection. Partial/blocked updater output cannot be promoted by generator success or unrelated written pages.
5. Root BEFORE evidence: astra-B1b-derived-repeat.json and astra-B1b-repeat-diff.json. Actual applied nine-page public vault produces three calls and three unnecessary updates solely because peer/self related context changes. Preserve these files; make a NEW real pipeline fixture with receipts, then prove first immediate finalize repeat is 0 calls / 0 pages / same current file IDs/revisions/bytes. Do not use an old pre-receipt vault as proof of a new-receipt repeat, and do not count a second repeat that only happens to settle.
6. Focused regressions: normal init/finalize pending and applied repeat; explicit zero; rejected; changed question/model/config/generator contract; new eligible source affects cumulative closure; real new external related card invalidates while own cohort does not; cached SQLite high-hit own outputs do not crowd out prior context; manual card edit invalidates; source/seed regressions preserved.
7. Demonstrate three INPUT batches on public/synthetic fixtures (including one failed input and a normal continuation) against all-at-once typed generation using fixed mock responses and normalized semantic identity/claims/evidence. Do not substitute three subsets of one already-generated plan. Same-vault repeats preserve IDs/revisions/bytes. Across independent vaults ignore random IDs, timestamps and transport order. Originals unchanged. If current whole-run review prevents a needed partial-approval case, identify it for B3 rather than bypassing authority.

Deliver bounded checkpoint and STOP for root acceptance. B3 plan already exists and requires separate dispatch. Live semantic/private-demo acceptance is not implied.