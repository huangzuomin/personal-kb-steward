# T7 first implementation review

Astra reviewed actual initial module code, not just the report. Recheck any fixes already made. Same owned files and public-only boundaries. Resolve before module acceptance.

Astra deliberately interrupted the initial session at the review checkpoint; all files remain. Continue current implementation and tests, do not start again. Run only topic/claims/contracts targeted checks; no entire tests directory. Keep permission controls and no compound shell cleanup. Finish bounded corrections and report.

1. Canonical schema must be loaded from core/schemas/topic-page.schema.json, not duplicated as a 100+ line Python _SCHEMA. Register the loaded file and validate through shared helper. Clear single source of truth.
2. Model output contract omits evidence.relation, but compile_claims requires supports/contradicts. Add exact valid protocol and a regression using only fields actually requested by the prompt; a response following the prompt must pass. Do not make all mocked responses silently richer than protocol.
3. `_budgeted_context` silently slices cleaned body per note; this then claims coverage full and checks quote positions against unseen full source. Use full normalized original snapshots (including full-file locator semantics) or explicit bounded source-unit context with matched positions. Reject oversized input before calls; never call full after truncation. Check full JSON payload size including question/known_paths, not only sum(body). Missing source key / invalid budget / malformed result should return a bounded documented error, not inconsistent uncaught exceptions.
4. Current is_full depends only on input path count/nonempty bodies, regardless of zero valid claims or missing source map/boundary/gaps/actions. A full topic cannot contain '(无已编译判断)' after all evidence is rejected, nor a blank map you auto-fill. Fail/drop unusable results or explicit insufficient stub, never claim full quality. Source count must be related, usable, distinct original sources actually selected for the question; 3 supplied docs with only 1 relevant is not a full topic.
5. Coverage is read coverage, source sufficiency is a separate state: a fully read single source is a full-coverage insufficient-source stub, not coverage partial merely because fewer than 3. If supplied sources are refused, retain that incomplete input diagnostic and do not magically regain full coverage by counting only survivors. Handle shared provenance metadata as well as duplicate hashes; don't assert independence just because hashes differ.
6. Tension sources must belong to verified sources and sides must refer to distinct compiled claims with real evidence. Duplicate claim_statements cannot inflate two-sided support. Two agreeing claims across two sources do not substantiate a real_conflict label. Use explicit sides/contrast and distinguish documented interpretation disagreement from factual contradiction/context difference. Reject unknown paths in tensions/map/freeform fields; no unverified paths disguised in Markdown.
7. Persist versioned claims AND analysis in actual rendered Markdown-owned metadata/block, not only return dict. Test parse_frontmatter -> read_claims/validate_claims from disk. The initial template/context drops these despite docstring claiming persistence. Include indexed knowledge objects with type/relationship when known; no invented future links.
8. Model summary/theme_boundary/tension text can carry fabricated [[wikilinks]] bypassing `_split_links`. Validate all rendered links against known actual source/object paths or render freeform content as literal safe text. Pending text must remain non-link syntax even if model supplies nested brackets. Path validation must reject absolute/traversal/backslash/control paths, not just []|#.
9. Next actions need ordering/priority plus a real gap reference; currently missing/unknown gap silently becomes None. Empty/generic actions cannot satisfy full contract. Validate types and preserve actual issue details in visible quality section; a lone `topic_generation_issues` token is not a readable limitation report.

Tests must exercise these discriminating failures plus the real synthetic 3-source family, single-source stub and unrelated zero. Preserve full raw sample model payload next to rendered fixtures. Keep all model calls mocked. Report exact evidence; real semantic acceptance remains later.

Shared persistence decision: rendered frontmatter `card_state: {version: 1, type: "topic-page", claims: [...], analysis: {...}}`. Use existing `_patch_header` after template render if needed; do not edit shared base template. Source cards analogously own info_units + analysis. Test reload and validate_claims against the same original snapshots. The returned claims/analysis and persisted state must match.
