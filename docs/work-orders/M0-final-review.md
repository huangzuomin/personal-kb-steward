# Astra M0 review — trust boundary, previews and configuration

Same Claude M0 owner/session, normal permissions and public-only data boundary. Implement these narrow corrections, not a new framework. Do not change T9 CLI/runtime files. Preserve concurrent T0 fixtures.

1. **Untrusted provenance retained:** prepare_card_item uses setdefault for source_hashes and coverage. Generic runtime passes model items directly, so a model's invented hash and coverage='full' survive. Astra reproduced this in docs/iteration-evidence/astra-metadata-provenance-repro.log. Distinguish trusted producer-supplied provenance from model fields, e.g. explicit keyword params for trusted source hashes/coverage. Without such capture overwrite to {} / unknown; never hash cleaned text and call it original bytes. Include a real run_skill_runtime mock-provider test supplying forged hashes/full coverage and assert they're not promoted. Preserve trusted producer snapshots when explicitly provided.
2. **Preview drops metadata:** core/renderer.py::render_preview still discards schema_version/generator_version/analysis_mode/source_hashes/coverage and quality flags, although generic runtime now prepares them. Preserve these for managed cards without changing unrelated preview semantics. base_frontmatter.j2 also currently omits quality_flags while docs/card-contracts.md claims they are persisted. Fix actual rendering and test parse_frontmatter of both executor output and generic preview, including quality_flags. Don't just update the documentation to lower the required behavior.
3. **Prompt/contract mismatch:** generic runtime output_contract still lists only old generic required keys; canonical source schema additionally requires key_statements/topic_hints. Provide managed type content requirements in the actual model prompt (program-owned metadata should not be requested from the model). Test captured prompt/payload for required source/seed content fields and valid source response through the real runtime. Do not rely on mocks supplying fields the prompt never asks for.
4. **Invalid explicit config values:** seed_generation_mode currently coerces false/0/empty mode to default via `or 'atomic'`. Default only when field is absent; reject present invalid types/values. Enforce mapping type for seed_generation. Keep core helper and validate_config behavior aligned. Test missing, atomic, topic, invalid string, false, 0, empty, non-mapping.
5. Check invalid seed item handling: executor currently catches validation errors and skips pages but may still return all inputs and processed=len(notes). Prove with a real plan/state path that invalid typed output cannot advance processed-index; if not, fail that generation attempt or exclude failed inputs with explicit errors. Valid zero-output/no-substance processing is distinct from schema failure. Avoid changing CLI ownership: fix producer result/error contract.

Run focused contract tests plus relevant PR29/source/seed/runtime tests under inherited UTF8. Update docs/card-contracts.md/report.md/report.json with exact semantics and results. Keep public-fixture task separate; atomic/source semantic generators remain M1. Stop for Astra acceptance; do not claim full product complete.
