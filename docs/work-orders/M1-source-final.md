# M1 source final acceptance corrections

Same source ownership/privacy boundaries; preserve seed/topic changes. Finish these focused items and regenerate public sample/report before handoff. Do not rerun the entire tests directory.

Astra stopped the prior review session at this checkpoint; edits remain. `astra-source-second-probe.json` now confirms all-invalid-quotes processed=0/status=partial, so preserve that fix (item 1 may already pass). The same probe confirms source-specific limitation text is still dropped. Complete the few remaining code corrections, then write the handoff. Do not get stuck debugging seed tests or shared pipeline internals; report a shared integration need explicitly. Use standard skill_executor.load_executor in tests instead of a second loader if renderer-module cache collisions arise.

1. Recheck all-invalid-quotes outcome. In reviewed revision, invalid quotes only set flags, but data.status is still ok when chunk calls succeeded and errors is empty; execute then increments processed. Zero usable units must never be a successfully processed source card. If useful diagnostic card retained, explicit blocked/partial/unusable state and processed=0; test real execute -> result, not just helper facts empty. Preserve valid explicit no-substance zero separately.
2. Retain substantive model limitations. Current analyze_note overwrites merged['limitations'] with only generic mode/range messages. Preserve safe source-specific limits and display them. Main key statements must display per-unit attribution, not only a top-level list of all speakers. The model's speaker label must match the nearest active explicit dialogue label/role, not merely occur somewhere in preceding 120 characters; mismatch becomes unknown/review and cannot be persisted as trusted speaker. Add dialogue with adjacent different speakers to prevent attribution swapping. These checks establish textual attribution, not speaker real-world identity.
3. Complete-render unknown wikilinks must not escape through model statement/topic/summary/limitation text. Pending concepts stay literal text. Only the explicit actual source path is linkable in this producer unless known real paths are supplied. Validate original path as canonical relative path. Preserve verbatim evidence in structured state, but render evidence/free text safely (do not alter quote used for verification).
4. Update old source test fixtures only where interface legitimately changed: you additionally own `test_executor_records_unknown_coverage_instead_of_inventing_hash` in tests/test_card_contracts.py (missing snapshot is now blocked; assert no forged provenance), and source-specific valid-input stubs in tests/test_render_fail_closed.py so they use valid exact snapshot + new chunk-response contract before deliberately failing render. Retain failure-is-not-model-fallback and dependency-preflight-no-model assertions. Do not fix seed tests or unrelated modules.
5. Include reports/logs, rendered PUBLIC actual baseline dialogue and uncited report mock samples and their raw structured responses. Persisted card_state and returned analysis must agree; no claims of real model semantic acceptance. List all remaining shared CLI/processed limitations precisely for integration. Clean only your known temporary debug file via owned file edit/delete tools; do not issue compound shell deletion commands.

Required independent review evidence: new negative checks above + source quality suite + source-specific contract/render tests + source traceability. Use single `python -m pytest ...` commands, UTF-8 environment already set.
