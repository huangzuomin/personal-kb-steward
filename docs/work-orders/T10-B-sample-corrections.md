# T10 B review — fix mock exemplars before runner reuse

NOT DISPATCHED. Resume T10 worker. 14 independent tests pass, but root rejects current example semantics. Same bounded public-only/no-live/no-other-worker-files restrictions. Own public_round_support.py, test_public_round_support.py, T10 checkpoint B report/new samples. Do not change generators. Root preserves failed samples under historical-failed-B.

Actual issues:
1. `_judgments()` has a fact source-mechanism claim AND an inference using the SAME Q.case_mech quote. `by_quote` dictionary silently retains the LAST judgment. Both concept.definition_claim and case.reusable_mechanism_claim point to the inference, and case reusable/inference display IDENTICAL. Select by explicit stable test-response role keys/statement constants, never quote->one-judgment map when quotes can support different judgments. Keep both roles distinct. The mechanism definition should define the three-stage chain from source text, not an untested causal effect. The explicit counterexample scope is an attributed source claim (fact-kind, still unverified), not a newly invented inference; preserve separate truly inferred commentary. Do not relabel every inference fact to make tests green.
2. Seed currently selects user_units[0] = only “我发现一个怪现象。” and gives generic growth (“补记一条具体下一步或反例”). It selects AI proposal ONE while appending proposal THREE's tail boundary without citing the tail unit. This fails the actual rubric despite substring tests. Select the concrete user insight about collecting unrelated to writing targets (or thinking not retained), and proposal THREE's reverse-filter idea with its own stable-topic/possible mistaken deletion/tentative-state boundary. Reference all actual required unit IDs. Specific growth step must follow this thought, e.g. try mapping next collected materials to a named writing topic and review wrongly excluded long-term material; no quota-filling generic growth. Actual quote locator/speaker verify through generator. Use readable titles, not first 110 characters with nested speaker markup.
3. Source prep used analyze_note only and does not render source cards. Add actual source executor render/execute path with four providers total; preserve per-source analysis and rendered source Markdown evidence. It is still NOT pipeline review/apply. Save readable Markdown samples for all five types, keeping raw JSON/prompts separately. Don't claim source heuristic fallback exercised by this success-only fixture.
4. Tests must assert definition expected source role, reusable != inference with correct bound kinds, user insight actual content and AI tail evidence, thought-specific growth. Replace permissive gap/action substring fallbacks with exact structured array checks. Keep mock data ONLY in test provider; public/live runner may never load answers. Current blanket scripts string scan will need scoped adjustment when a future runner explicitly lazy-loads mocks ONLY under --smoke; do not weaken live-mode import isolation now.

Regenerate current offline-round artifacts only after preserving root historical directory. Before replacing files verify paths stay within T10/offline-round; prefer versioned new offline-round-v2 directory to avoid deleting old files. 8-call actual count must remain. Focused tests + report truthful limitations. Stop for root re-review.
