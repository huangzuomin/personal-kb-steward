# T10 adapter review corrections — exact real invocation

Same adapter ownership/restrictions. No live calls; no generator/pipeline edits. Root inspected implementation before a real request could be wasted. Correct these concrete interface issues and add decisive mocked-invocation regressions:

1. Native argv MUST include `-p`/`--print`. Current build_argv lacks it; --output-format json is print-mode output, and unattended provider calls must not launch interactive mode. Existing successful coding invocations use claude -p. Test required print flag, tools-disabled, strict MCP, no resume, native shell=False and isolated cwd.
2. Preserve the actual generator JSON payload. Current compose_payload/to_json wraps inputs as {documents,task} and adds full documents. That changes the source/chunk and seed/unit contracts and invalidates prompt-equivalence evaluation. Add explicit registration of the EXACT existing generator payload with its public source provenance (run harness is trusted to construct it from allowlisted fixtures/registered public intermediates). Serialization may canonicalize key order but must preserve deep JSON equality with the generator argument and add NO wrapper or extra document fields. Snapshot/hash registration is provenance accounting, not proof that arbitrary free strings are public; document trusted-caller boundary honestly. Protect manifest expected_* annotations from accidental nesting, not just top-level task keys. Tests include different real shapes: source chunk dict, seed units list, concept/case/topic documents payload. Use these public shapes; do not read private inputs.
3. Provider callable receives production cfg dict from generators. Do not treat it as permission to override adapter settings; IGNORE its values and always use frozen adapter config, or provide documented bind_for_round wrapper compatible with `(cfg,system,payload)` and `(system,payload)` generators. Current identity requirement `cfg is self.config` is incompatible with the advertised generic provider seam. Round selection must remain explicit, not all calls round=0.
4. CLI envelope result must be an actual string. Current str(envelope['result']) silently converts None/dict/list and claims exact text. Reject nonstrings with preserved raw evidence. Nonzero/is_error/timeout count against budget. Boundaries tested before subprocess launch, including malformed config (positive integer call counts, positive timeout, explicit bool live flag) rather than bool('false') enabling live.
5. Keep raw evidence and failures without overwriting run/attempt files. Verify real native executable resolution is platform-valid from npm shim (already given), no shell wrappers. No need to expand scope into a generic security framework.

Run tests/test_public_evaluation_harness.py only, update docs and checkpoint-A report with actual invocation contract and tests. All tests remain mocked; no live call from coding agent.

Native CLI help verified again by Astra (2.1.278): `--restricted` removes code-running tools and WebFetch unless --tools names them, ignores user/project/local settings (managed settings and --settings still apply), and supports --strict-mcp-config alongside it. Add --restricted to the public evaluation invocation together with --tools "", --disable-slash-commands and --strict-mcp-config. This provides the intended no-project/no-user-settings execution without --bare's OAuth breakage. No --settings or --add-dir needed. Test this flag is present. --setting-sources is also listed in installed help but redundant under restricted mode.
