# T10 hard gate — decodable but damaged text

NOT DISPATCHED. Resume source worker when a slot opens. Root found an actual gap after previously accepted generator checks: tests used source_error/invalid UTF-8 only, but canonical public neg_damaged.md is valid UTF-8 containing U+FFFD. Source executor still called provider once, emitted one created card and processed=1 with only generic unknown-source warning. Evidence astra-damaged-text-before.json and ignored root .execution/astra-damaged-text-probe.py. Preserve before evidence.

Own NEW core/text_integrity.py, tests/test_text_integrity_gates.py; NARROW calls in skills/topic-research-compile/executor.py, core/atomic_seed.py, core/evidence_cards.py, core/topic_generation.py and source renderer only if necessary. No other edits (pipeline/CLI/update helpers owned by others). Keep all existing APIs. No private/parent vault, live config/secrets, model/network calls, installs, subagents, commits/pushes. Public canonical fixtures and local deterministic providers only.

Reject/explicitly block source strings containing Unicode replacement character U+FFFD before provider invocation. Do not repair or remove characters, normalize hashes or reconstruct damaged meaning. Apply at all five generator entry seams, including supplied atomic units/snapshots path. A mixed batch cannot be labeled full/complete if any input is damaged; preserve truthful per-input issues and unaffected-source provenance. No false processed success. Source may return blocked analysis with visible damaged_source reason, no card. Source original bytes remain untouched.

Also reject U+FFFD in returned MODEL text before parsing/accepting/rendering; full raw response stays in evaluation recorder, diagnostics need not echo corrupted text. Returned malformed/encoding-damaged response is error, not valid zero or full. If existing fallback policy retains a clean heuristic preview, it must remain clearly non-llm/non-complete/processed=0 and non-eligible for promotion. Do not introduce fuzzy Chinese mojibake detectors or blanket rejection of ordinary '?' punctuation. Legitimate valid Unicode Chinese, BOM/CRLF, emoji, rare CJK and code with question marks stay valid. Implement minimal shared check instead of changing unrelated secret-scanning policy.

Tests ACTUAL canonical neg_damaged.md bytes with computed matching hash, provider sentinel proves zero calls; raw invalid-byte adapter case remains covered elsewhere. Test source, atomic ordinary note + supplied units, concept/case shared entry and topic input. Test a valid synthetic input whose provider returns parseable JSON containing U+FFFD in a field (including unused nested field), no accepted full output. Test positive Unicode boundaries and non-mutating input bytes. Rerun affected generator/traceability tests, note precise counts/skips. Do not claim semantic quality from these gates.

Return compact report and after evidence separately. STOP for root review; no pipeline changes or live evaluation.
