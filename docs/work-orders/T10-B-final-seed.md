# T10 B final narrow seed correction

DISPATCHED. Resume T10 worker, same public-only/no-live/no-subagents/no-commit constraints. Own same support/tests/evidence; narrow additional ownership GRANTED skills/mindseed-grow/renderer.py for the newline defect only (seed generator/updater remain untouched).

Root independently reran support 15 passed and read actual seed Markdown in v2. AI card now correctly cites proposal THREE + tail boundary. Two remaining sample defects and one real renderer defect:

1. User card quote supports collection/writing-target mismatch only, but core summary adds “存了笔记不等于存下了思考”, a separate thought from the opening turn not cited. Keep this atomic card ONLY about mismatch; don't add another thought/quote just to keep the slogan.
2. User card negative_scope says “不适用于选题经常漂移的场景”, which belongs to AI reverse-filter METHOD, not the user's observation. Replace with precise boundary: records Lin's self-report only, cannot infer notes always harm output or generalize to other people's collecting practice. Growth should stay specific to identifying the intended writing use of a newly collected material; AI proposal handles tentative-state/quarterly filtering details separately. Model-proposed growth is allowed but don't attribute it as historical user action.
3. ACTUAL seed renderer concatenates evidence list rows with ''.join(rows); two quotes appear as `...）- 提案边界...` on ONE line. Fix newline joining in renderer.py narrowly; retain raw machine evidence/hash/state. Add actual multi-evidence rendering test proving two distinct Markdown list lines without changing claim contents. This is a production display defect, not just mock polish.

Regenerate versioned offline-round-v3 (keep v1/v2/historical), report current final reference. Use readable artifact filenames (avoid .md.md suffix if convenient). Run support + seed quality/atomic focused tests. Public round still EXACT8 calls and no answer injection; pipeline/review/apply/live remain pending. Stop for root acceptance; do not start runner.
