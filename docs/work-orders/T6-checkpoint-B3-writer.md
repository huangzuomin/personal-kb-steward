# B3 writer/queue integration checkpoint

Root scheduling decision: after independently verifying B2-derived core (78 passed, 1 native symlink privilege skip), allow the independent writer/queue portion of B3 in parallel with the remaining B2 lifecycle tests. This supersedes the earlier blanket NOT DISPATCHED/B2-complete prerequisite only for this owned portion. Full B3 acceptance still depends on B2 and receipt integration.

Owner: public_runner_finish. Worktree .pks-iteration. Public/synthetic only. Preserve others' changes. No private vault/config.json/.env/secrets/live/native model/network/install/commit. No full suite during parallel edits.

Own core/review_runs.py, core/review_queue.py, core/run_records.py, the existing scripts/personal_kb_steward.py apply/save-review seams, optional narrow core/apply_execution.py extraction, focused NEW tests/test_subset_apply_integration.py, report M3-integration/checkpoint-B3-writer.md/json. Existing B3a/B3b helpers are accepted; only change them for a demonstrated integration defect reported explicitly. Do not change core/pipeline_history.py, core/card_pipeline.py, initializer/finalizer, semantic generators, or independent tests/test_subset_apply_blackbox.py. History owner is topic_integration_finish; receipt integration follows later.

Read T6-checkpoint-B3-subset-review.md completely, including root real-manifest BEFORE and independent desired test notes, and final Astra overrides in M3-integration/B3-architecture-proposal.md. These govern authorization, failure evidence, immutable plans and single writer. CLI <=1700 lines; use extraction if needed, preserving existing monkeypatch seams when reasonable rather than replacing the writer implementation.

Concrete target:

1. New saved bound plans declare a page-scoped review contract. Queue creation emits exact page_review_records for proposed writes. Replace only the redundant generated-page aggregate review with those records; preserve genuine non-page blockers. Deleted/missing new authority rows never invoke legacy whole-run fallback. Legacy aggregate handling is only for genuinely old plans.
2. review apply-approved selects only exact approved pages through select_page_review with verified actual existing outputs. Honor can_apply_subset and authorized_targets; selected_pages alone is not authorization. Revalidate queue at write boundary and detect post-write conflict.
3. Keep immutable saved parent plan/hash and bound page bytes/object IDs/revisions/base authority. A distinct reviewed target set gets a protected deterministic audit identity bound to parent and subset_evidence_hash. Repeated successful selection is a verified no-op. Do not mint suffixes to replay failed selections or bypass assert_run_can_start/save_run_manifest.
4. Execute selected pages using the existing preflight/write_observed_page/reconcile/state/log/manifest path. Link both success and failure manifests to exact parent_run_id/parent_plan_path/parent_plan_sha256/subset_hash/selected_targets. The accepted B3b reader requires successful reconcile.ok=true and observed/current exact mutation proof for failed individual writes; never promote a failed batch to applied.
5. Queue updates only exact approved rows whose current output bytes/identity verify, including individually proved writes in a failed attempt. Pending/rejected siblings remain unchanged. Explicit later continuation may select a distinct safe remainder after observed-success reconciliation; unverified existing writes stay blocked. No cross-process transaction claim.
6. Preserve processed-index truth: a subset must not mark input complete merely from broad page.sources membership. If receipt closure integration cannot be completed without history owner's files, conservatively leave incomplete source processing unadvanced and report the integration seam. Do not falsely mark all inputs complete. B2 reuse after subsets is a later joint integration check.

Checks: independent desired two-test module (currently fails because only aggregate queue exists), own exact authority/missing row/forgery/non-page blocker/future link/queue change/partial failure/reapply tests, relevant existing review/run/producer recovery tests. Actual producer -> bound save -> review -> existing apply fixture required. Preserve originals and parent bytes. Root BEFORE actual-manifest probe verifies 2 normal outputs, but a failed second write has missing plan hash linkage; correct production failure metadata rather than weakening the reader.

Deliver a bounded writer checkpoint and FINAL handoff. If substantial extraction is needed, first deliver the concrete API/file design in report and stop at a safe checkpoint; root will resume without user permission ceremony. Do not claim full B3, live semantics, deployment or stable release.
