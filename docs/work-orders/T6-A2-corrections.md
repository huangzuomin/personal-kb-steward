# A2 root review corrections — acceptance blocked

NOT DISPATCHED. Resume same integrator; preserve scope and other workers. Root reviewed diff/new code and actual tests. Before evidence: astra-A2-probes-before.json, ignored .execution/astra-A2-probes.py. Preserve before file; after output must use another path. No private/live-model/network/install/subagents/commit operations.

Four reproduced failures:
1. --no-llm returns provider_calls=1 for both concept/case. Count ACTUAL invocations by wrapping the chosen existing provider seam (including default provider), not inferred from state/reason strings. Disabled, invalid config, no inputs, renderer/secret/oversize preflight blocks all 0; a provider attempt which raises counts 1. Distinguish pre-call error from model_error in stage reason. Real call count tests, not state-only assertions.
2. Planned pages lose the structured item although A2 explicitly required retaining it. Add page.item exact candidate and retain card_state content; no renormalization. The parallel T8 helper consumes this to bind definition/mechanism identity. Source adapter may also retain exact item if available; do not invent it by parsing prose.
3. known_paths sends the ENTIRE index; probe added 35 irrelevant notes, all 35 reached each model. Use existing Retriever bounded selection for related candidates (e.g. <=6 or configurable bounded cap) based on meaningful eligible titles/hints, existing confirmed targets only. Exclude input originals from related candidates as appropriate; all emitted links still validate. Pin retrieved context hashes if used, in addition to upstream+original snapshots. Pin ALL eligible original/source-card snapshots actually supplied in that generator call, not only item.sources: currently uncited inputs still appear in the model context/provenance map and can influence output. Keep item.sources/source_hashes limited to actual cited evidence; retrieval_source_hashes is the larger generation-input dependency set. Don't build a second scan/index or truncate source text; oversized source set still explicit block. Test irrelevant filename exclusion, actual search report/candidate reasons, finite cap even on large index, and changed previously supplied-but-uncited original before apply blocking. Prefer injected request-scoped retriever or one per discovery; no per-candidate index rebuild.
4. Case skill is unreachable through actual task routing: route('生成案例卡') -> mindseed-grow. Legacy alias to prepare_writing is not a case-generation route. Narrow ownership GRANTED for router.json/workflows.json/core.router only if necessary. Add clear case generation entry/keywords ordered to avoid generic '项目' taking '项目案例卡'; preserve generic work-memory/writing flows. Test actual command_plan/make_execution_plan with user case request, patched provider sentinel, resulting primary_skill/case pages/stage visibility. Direct mvp_executor_plan(skill='case-story-bank-builder') alone doesn't prove CLI routing.

Additional acceptance evidence gaps:
- Existing stale-source/card tests change before DISCOVERY. Add genuine save reviewed downstream plan -> change original OR upstream card -> apply-approved; assert rejected before ANY write, cards absent, original deliberate test mutation retained, queue not falsely applied. Existing generic pin validator should enforce this, prove at actual seam.
- 'reject sibling' test uses TWO separate runs; not same-plan subset review. Label report accurately as separate-run isolation. Same-plan partial approve/reject belongs B2; do not claim it complete now or weaken existing run gate in A2.
- Source/seed compile tests must actually exercise a typed provider and generic runtime sentinel. Current typed-skill test has no quick input and raw guard, so it can pass without generation. Add at least one real quicknote atomic or raw source path, known one typed provider call and zero generic calls. Respect explicit seed_generation.mode=topic legacy behavior when suppressing generic runtime; source/case typed always use their sole producer. State exactly what legacy behavior is retained.
- case executor exposes stages but mvp_executor_plan currently drops them. Preserve stages into plan actions or a clear typed stage field so no-LLM/zero/failure case command is observable, not just empty pages.

Run focused A2 + affected route/review/runtime/config checks; root will independently probe and verify. CLI <=1700, no broad rewrite. Report precise commands, current test count/skips and remaining B limitations. Stop for review; no B1 yet.
