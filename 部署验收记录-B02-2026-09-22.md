# 部署验收记录 — B02 标题不再被盲切成半句/半词（2026-09-22）

状态：**已部署，部署后验证通过。**

部署位置：`D:/OLD_VAULT-整理后-20260919/tools/personal-kb-steward`
来源：`.pks-iteration`（迭代区）
时间：2026-09-22 08:50–09:00

## 修的是什么

seed 卡标题出现半句、甚至半词：

- `在 AI 绘图工具快速迭代的环境下，如何既保持技能不快速过时、又不让重新学习的成`
- `半年未使用 Stable Diffusion，技术演进已使既有经验被淘汰，Com`  ← 英文单词被切成半截
- `笔记中并存两条水墨风格提示词，均以极简禅意自然景观为主题：一条用日出金光渲染巨石`

**根因**（`core/atomic_seed.py:289`）：

```python
title = _single_line_text(raw.get("title"), 60) or statement[:40]
```

`_single_line_text` 是**校验**函数（空 / 超 60 字 / 含换行 → 返回 `None`）。一旦模型没给 `title`、
或给了超长的，就退化成 `statement[:40]` —— **盲切 40 字**。

## 改动

| 文件 | 改动 |
|---|---|
| `core/atomic_seed.py` | 新增 `_short_title(text, limit)`：取预算内**最靠后的**子句/句末边界收短；去掉悬空标点；绝不切断英文单词（回退到词边界）；只在真的硬切时才加省略号 |
| `core/atomic_seed.py:289` | 模型未给 `title` → `_short_title(statement, 40)`；模型给了但超长/含换行 → `_short_title(supplied, 60)`（**收短保留，不再整条丢弃**） |
| `core/atomic_seed.py`（`_preview_item`） | 同类盲切 `unit["quote"][:40]` → `_short_title(unit["quote"], 40)` |
| `tests/test_short_title.py` | 新增 11 项定向测试（合成样本，不含真实个人笔记） |

**为什么按「位置最靠后」而不是「标点强度」选边界**：先按强度（`。！？` > `；` > `，、` > `：`）试过，
结果在第 3 例上**反而变差** —— 它在第一个逗号（14 字）就收尾，丢掉了「均以极简禅意自然景观为主题」
这个要点。改成取预算内最靠后的边界后：

| | 旧（盲切 40 字） | 新 |
|---|---|---|
| 1 | …又不让重新学习的**成** | …如何既保持技能不快速过时（30 字） |
| 2 | …被淘汰，**Com** | …技术演进已使既有经验被淘汰（36 字） |
| 3 | …一条用日出金光渲染巨石 | …均以极简禅意自然景观为主题（28 字） |

**开发过程中被测试抓到的一个真 bug**：硬切时加了省略号却没给它预留位置，
导致标题 61 字超出 60 上限。已修（硬切改用 `limit - 1` 的预算）。

## 同步范围

`core/atomic_seed.py`（覆盖）、`tests/test_short_title.py`（新增）、`LOCAL-PATCHES.md`（覆盖）。
**只增改、不删除。**

## 验证清单结果

| # | 项目 | 结果 |
|---|---|---|
| 1 | 全量备份 | 1128 个文件 → `evidence/deploy-B02-20260922-0850/backup-20260922-0850` |
| 2 | 3 个文件哈希 = 源哈希 | **3/3 一致** |
| 3 | 部署侧独有资产 488 项 | 变更 **0**、丢失 **0** |
| 4 | 配置兼容性（新代码 × 旧 `config.json`） | `validate_config.py` 退出 **0** |
| 5 | CLI 冒烟 `--help` | 正常 |
| 6 | **部署后定向门禁**（`test_short_title` + `test_atomic_seed` + B07 那 8 个相关文件） | **158 passed in 131.46s** |
| 7 | 部署后全量回归（排除 `test_ci_committed_diff.py`） | **1004 passed / 89 subtests / 8 failed in 329.50s** |
| 8 | **相对部署前的新增失败** | **0**（失败集合与已知 8 项逐条相同） |
| 9 | 原始知识库未动 | 今日被修改/新建文件 **0**，`_kb-steward/` 仍 **349** 个文件 |
| 10 | `.openclaw/` 今日新增 | 3（1 个新 plan + queue + operation-log，均来自本日早前的 dry-run，非本次部署） |

## 关于那 8 个失败（与本次修复无关）

与 B07 那轮**完全相同**的 8 项符号链接权限类测试。`2026-09-21` 基线里它们是被 skip 的 9 项，
现在符号链接能建了 → 断言反转为 failed。已在**未改动的部署代码**上复跑确认（8 failed in 2.05s）。
详见 `部署验收记录-B07-2026-09-22.md`。

## 回滚

```bash
python iteration-artifacts/2026-09-21-live-realgate/deploy_patch.py  # 不用于回滚
```

按文件从 `backup-20260922-0850` 还原 `core/atomic_seed.py` 与 `LOCAL-PATCHES.md`，
并删除新增的 `tests/test_short_title.py`。**不要用 `/MIR` 整体镜像**（会抹掉部署后新数据）。

## 部署后仍未做的事

- **未在生产库发起新的真实任务**。本日早前那次 dry-run（run `20260922-082521-975114`）产出 3 页，
  已在审核队列中待用户决定；未执行 `apply-plan` / `review apply-approved`。
- B06（写作入口）仍未验证（两次被服务端内容过滤拦下）。
- 存量卡片不修复、不全库重跑。

## 证据

- `iteration-artifacts/2026-09-21-live-realgate/evidence/deploy-B02-20260922-0850/deploy-report.json`（verdict: PASS）
- 同目录 `post-deploy-gates.log`
- `iteration-artifacts/2026-09-21-live-realgate/evidence/B02-verify.log`（部署前验证）
- `iteration-artifacts/2026-09-21-live-realgate/生产首次dry-run记录.md`
