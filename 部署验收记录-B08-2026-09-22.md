# B08 部署验收记录（2026-09-22 11:52）

**补丁标签**：`B08`
**补丁内容**：① 人工拒绝不再阻塞整轮记账（P0）｜② 中文查询切词修复｜③ 检索范围可配置
**部署工具**：`iteration-artifacts/2026-09-21-live-realgate/deploy_patch.py`
**命令**：
```
deploy_patch.py B08 core/apply_execution.py core/state.py core/retrieval.py \
    scripts/personal_kb_steward.py tests/test_rejected_page_accounting.py \
    tests/test_retrieval.py config.example.json LOCAL-PATCHES.md
```

---

## 一、部署结果：verdict = PASS

| 步骤 | 结果 |
|---|---|
| A 源哈希 | 8 个文件 |
| B 全量备份 | **1139 个文件** → `iteration-artifacts/2026-09-21-live-realgate/evidence/deploy-B08-20260922-1152/backup-20260922-1152` |
| C 同步前独有资产 | 497 项 |
| D 复制 | 8 个（7 updated + 1 created） |
| E 哈希复核 | **8/8 一致**（`hash_mismatches: []`） |
| F 独有资产 | **变更 0、丢失 0** |
| verdict | **PASS** |

部署报告全文：`evidence/deploy-B08-20260922-1152/deploy-report.json`

## 二、8 个文件与哈希（部署后 = 迭代区，逐条一致）

| 文件 | 动作 | sha256（前 12） |
|---|---|---|
| `core/apply_execution.py` | updated | `a4789c0e8f5d` |
| `core/state.py` | updated | `f713e91921c2` |
| `core/retrieval.py` | updated | `0d5e46cacb6e` |
| `scripts/personal_kb_steward.py` | updated | `9a0b4b2f7b12` |
| `tests/test_rejected_page_accounting.py` | **created** | `ac5946d02087` |
| `tests/test_retrieval.py` | updated | `9ea4cf081372` |
| `config.example.json` | updated | `87ad459127e6` |
| `LOCAL-PATCHES.md` | updated | `54b3b1734e08` |

## 三、部署后三件核验

| 核验项 | 结果 |
|---|---|
| ① 哈希逐条一致 | **8/8** |
| ② 独有资产变更 / 丢失 | **0 / 0**（497 项基线） |
| ③ `git status` 删除行 | **0**（部署前 129 项 → 部署后 131 项，未跟踪 82 → 83） |

**8 个文件在 git 中的状态符合预期**：6 个 ` M` + 2 个 `??`（`core/apply_execution.py` 与 `tests/test_rejected_page_accounting.py`）。

> 说明：生产目录是本地定制版，`git status` 长期有 129 项本地改动，因此它不是「本次部署是否干净」的基准。
> 判据是**删除行 = 0**（`deploy_patch.py` 只增改不删除）与**独有资产 0 变更 0 丢失**。

## 四、生产 CLI 可用性

| 命令 | 结果 |
|---|---|
| `scripts/validate_config.py` | 退出 0，配置校验通过 |
| `scripts/personal_kb_steward.py --help` | 退出 0 |

`validate_config.py` 输出确认知识库仍正确指向 `D:\OLD_VAULT-整理后-20260919\OLD_VAULT`。

## 五、部署后门禁

### 5.1 定向套件（生产目录）

```
pytest tests/test_rejected_page_accounting.py tests/test_retrieval.py \
       tests/test_object_plans.py tests/test_claim_evidence.py \
       tests/test_synthesis.py tests/test_review_guards.py -q
```

**1 failed, 76 passed, 26 subtests passed in 9.32s**

唯一失败：`tests/test_review_guards.py::ConfigReviewTests::test_symlink_escape_rejected` —— 属已知 symlink 类（见 5.3）。

### 5.2 全量回归（生产目录，排除 `test_ci_committed_diff.py`）

```
pytest tests/ -q --ignore=tests/test_ci_committed_diff.py
```

**5 failed, 1015 passed, 89 subtests passed in 405.06s（6m45s）**

失败 5 项：

1. `tests/test_derived_index.py::test_links_do_not_export_external_notes_or_redirect_cache`
2. `tests/test_public_baseline_runner_c3.py::test_prepare_vault_rejects_prepopulated_or_escaped_target`
3. `tests/test_review_guards.py::ConfigReviewTests::test_symlink_escape_rejected`
4. `tests/test_seed_quality_outputs.py::test_configured_auxiliary_directory_symlink_is_rejected_before_apply`
5. `tests/test_windows_write_boundaries.py::test_true_symlink_guard_evidence_or_explicit_unverified`

### 5.3 对照实验：这 5 项失败与 B08 无关（关键证据）

从**部署前代码**（即本次全量备份目录 `backup-20260922-1152`，已核对 4 个核心文件哈希 = 部署前值）跑同一组 5 项：

```
cd <backup-20260922-1152>
pytest <上述 5 项> -q
```

**5 failed in 2.60s** —— 失败集合与部署后**逐条相同**。

→ 这 5 项是本机 **Windows 目录符号链接行为漂移**导致的环境问题，与 B08 改动无关。

### 5.4 关于「少了一项」的说明

迭代区基线曾记录 **6** 项 symlink 失败，本次生产侧全量为 **5** 项，少的是
`tests/test_public_baseline_runner_c3.py::test_main_refuses_symlink_artifact_root`。

单独复跑该项 → **1 passed in 0.37s**。它是**负载/时序相关的 flaky**（全量并发下偶发失败），
不是被 B08「修好」的，也不构成新增失败。

### 5.5 门禁结论

| | 结果 |
|---|---|
| 部署前已知失败（symlink 类） | 5～6 项，flaky |
| 部署后失败 | 5 项，**全部落在已知集合内** |
| **新增失败** | **0** |

## 六、回滚方法

从备份目录按文件还原：

```
cp "<evidence>/deploy-B08-20260922-1152/backup-20260922-1152/core/apply_execution.py"        core/
cp "<evidence>/deploy-B08-20260922-1152/backup-20260922-1152/core/state.py"                  core/
cp "<evidence>/deploy-B08-20260922-1152/backup-20260922-1152/core/retrieval.py"              core/
cp "<evidence>/deploy-B08-20260922-1152/backup-20260922-1152/scripts/personal_kb_steward.py" scripts/
cp "<evidence>/deploy-B08-20260922-1152/backup-20260922-1152/tests/test_retrieval.py"        tests/
cp "<evidence>/deploy-B08-20260922-1152/backup-20260922-1152/config.example.json"            .
cp "<evidence>/deploy-B08-20260922-1152/backup-20260922-1152/LOCAL-PATCHES.md"               .
```

并**删除本次新增**的 `tests/test_rejected_page_accounting.py`。

**不要用 `/MIR` 整体镜像**（会把 `config.json`、`.env`、`.openclaw/`、`pr-artifacts/` 等本地独有资产一起冲掉）。

## 七、知识库影响

本次部署**只动代码**，未触碰知识库内容：

- `config.json`（PRESERVE）未变 → **③ 的检索范围扩展未激活**，行为与部署前一致
- `processed-index.json` 未变
- 知识库 `_kb-steward/` 产物零改动

## 八、遗留待办（需用户拍板，本记录不含执行）

1. **③ 是否真的开启扩展范围**：需改**生产 `config.json`**（不是 `config.example.json`）的
   `scan.retrieval_include_dirs`，建议值
   `["raw","quicknote","inbox","1-每日记录","2-领域","3-资源","4-项目"]`；
   `5-归档/`（301 篇）建议**不纳入**。覆盖率 29.2% → 86.2%。
2. **④ 凭据副本清理 + 库根加 `.gitignore`**：`iteration-artifacts/` 内 3 份 `.env` 明文副本；
   属破坏性操作，**需用户明确确认**。
3. **⑤ 跑第三轮**：等 ②③④ 定案后再跑，届时才可能吃到新内容。
