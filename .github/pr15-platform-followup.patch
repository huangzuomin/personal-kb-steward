diff --git a/skills/topic-research-compile/executor.py b/skills/topic-research-compile/executor.py
index 5603582..38ceee0 100644
--- a/skills/topic-research-compile/executor.py
+++ b/skills/topic-research-compile/executor.py
@@ -171,7 +171,8 @@ def execute(context: dict[str, Any]) -> dict[str, Any]:
             continue
 
         cleaned = clean_body(str(note.get("body") or ""))
-        text = f"Title: {note.get('title', '')}\n\n{cleaned[:int(cfg.get("scan", {}).get("max_source_chars", 6000))]}"
+        max_chars = int(cfg.get("scan", {}).get("max_source_chars", 6000))
+        text = f"Title: {note.get('title', '')}\n\n{cleaned[:max_chars]}"
         try:
             resp = call_chat_completion(cfg, system_prompt, {"text": text})
             data = json.loads(resp)
diff --git a/tests/test_pr15_contracts.py b/tests/test_pr15_contracts.py
index 77c7abe..66cb440 100644
--- a/tests/test_pr15_contracts.py
+++ b/tests/test_pr15_contracts.py
@@ -141,7 +141,8 @@ def test_finalize_does_not_claim_unrelated_sources_or_drop_custom_source_headers
     assert not ("UniqueClimateFinding" in topic["content"] and "UnrelatedPhotoFinding" in topic["content"])
     assert topic["review_required"] is True
     update = next(p for p in result["planned_pages"] if p["rel_path"] == original.relative_to(vault.root).as_posix())
-    assert "custom: preserve\n# preserve header comment" in update["content"]
+    newline = "\r\n" if b"\r\n" in original.read_bytes() else "\n"
+    assert f"custom: preserve{newline}# preserve header comment" in update["content"]
     assert update["review_required"] is True
     assert set(update["sources"]).issubset(update["retrieval_source_hashes"])
 
