# 部署验收记录 — 2026-09-21

状态：**已部署，部署后验证通过**。Codex执行同步、审核和验收。

部署位置：`D:/OLD_VAULT-整理后-20260919/tools/personal-kb-steward`。
来源：`.pks-iteration` 的完整已验收应用树，132个冻结文件与 `code-freeze-final.json` 一致。

| 检查 | 结果 |
|---|---|
| 完整备份 | 部署前971个文件逐一hash验证，含原配置、凭据与运行状态 |
| 白名单同步 | 371个文件纳入清单；更新47个、新增168个、不删除文件 |
| 最终代码 | 132/132冻结文件一致，371个同步清单文件hash一致 |
| 保留文件 | 751个非同步、非缓存文件hash未变，包含config.json、.env、.openclaw及部署侧独有资料 |
| 配置与CLI | 默认Python 3.13.2下validate_config、--help、只读status均退出0 |
| 部署目录完整回归 | **1003 passed，9 skipped，0 failed**；181.36秒 |
| 原始知识库 | 2727个文件的路径集合、大小、mtime_ns前后完全一致；未执行生产库写入或重跑 |
| 实际状态 | status读取2174份笔记、350条processed记录，自上次运行后的变更0 |

9个跳过项均为Windows符号链接创建权限限制，不将其报告为已通过。应用是按次调用的CLI，本次没有更换provider、修改活配置或启动新的后台服务。

## 部署代码的隔离流程验证

直接导入部署目录中的 `scripts/personal_kb_steward.py`，使用独立副本和此前保存的真实模型响应，未再次调用模型：

- 工作记忆：项目原件与日记均进入输入，生成3页；沿用内容审核判定批准2页、拒绝1页，仅2页写入。
- 写作材料包：摘要、全部复核项和待创建目标保留，生成并批准写入1页。
- 两场景均在审批前无页面写入；落盘内容与最终审核计划hash一致；再次应用均为 `terminal_noop`；输入副本hash未变。
- 部署代码的说话人控制检查通过：明确两轮对话保留归属，元数据与普通标题不产生假说话人。

这证明部署后的受控写入链路可用；不等同于新的实时模型调用或生产库批量重跑。模型内容仍需审核；原有存量卡片未修复。

隔离测试首次比较了绑定object_id前的页面列表，导致测试脚本的hash断言失败；修正为比较保存后的最终审核计划后通过。应用代码未因此改变；首次失败日志和隔离目录均保留，见 `isolated.log`，通过证据为 `isolated-v2.log` / `deployed-flows.json`。

## 同步边界和证据

同步应用代码、测试、技能、模板、示例、集成与配套文档；根级文件采用Git跟踪文件白名单。未同步`.execution`、迭代运行数据、缓存和旧`docs/iteration-evidence`产物。模板及示例中的quicknote等子目录正常同步。部署后测试新增的`docs/iteration-evidence/T0/fixture-hashes.json`为公开合成测试快照。

`LOCAL-PATCHES.md`已追加本记录入口。没有提交Git或推送远程。

- [全部部署证据](D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-21-deployment)
- [同步与部署前文件清单](D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-21-deployment/deployment-manifest.json)
- [最终完整性检查](D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-21-deployment/verify-final.json)
- [部署目录完整回归](D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-21-deployment/tests.log)
- [隔离流程结果](D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-21-deployment/deployed-flows.json)

## 回滚依据

完整备份：`D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-21-deployment/backup-before`。备份包含本地凭据，仅保留在本机。

如需回退代码，按`deployment-manifest.json`的`changed`从备份恢复，按`new`清单核对部署后hash再处理新增文件。保留部署后产生的业务数据、活配置和运行状态；不要直接用全目录镜像删除式还原。此次部署未发生回滚。
