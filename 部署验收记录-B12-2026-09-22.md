# B12 升级验收记录（2026-09-22）

**状态：B12 已于 2026-09-22 22:35 部署，生产目录验证通过。**

依据：《系统诊断分析-20260922.md》《部署验收记录-B11-2026-09-22.md》。本轮目标是消除可复现的批处理阻断和误记账，保持 B11 暂停 seed 的决定。

## 完成的改进

1. **正常源不再被降级源拖住。** 非 LLM 源页在进入写入计划前被隔离；失败原因、模式和可重试 receipt 保留。正常页仍经原有审核/预检写入；失败来源不计成功、不创建不可写页面审核项。旧降级计划的写入禁令保留。
2. **瞬时 provider 错误有限重试。** 默认最多 3 次尝试，剩余 300 秒预算限制后续调用 timeout 和等待。认证/请求错误、非法响应和安全失败不重试；错误正文和异常链脱敏。未更换模型或请求格式，未宣称真实延迟改善。
3. **初始化配置真正约束运行。** pipeline 声明来自 workflows；可省略 seed/promote，非法/重复/乱序声明明确失败；`seed_stage=false` 在选择 seed 历史和调用 provider 前即生效。首次待处理 raw 优先于失败重试，避免失败项长期占满批次。
4. **审核统计可对账。** 摘要区分 approved 与 applied，未知状态单列；新时间戳明确 UTC，历史队列不改写。
5. **启发式诊断更完整。** 恢复标题后正文，排除跨块代码、编码媒体、纯导航和空 HTML 媒体片段，引用坐标仍绑定原文。仍只作诊断，不获写入资格。
6. **外层批处理兼容补丁。** 部署检查发现当前 `scripts/run_init_batches.py` 没有把完整 run_id 传给 apply-approved，且不识别子集执行的 parent_run_id。仅补这两行；不代为运行批处理，不代为批准或应用现有计划。

报告引用的两项旧缺口已核实：当前 Jinja2 缺失会明确失败；source/seed 与 typed producer 已有运行时卡片契约校验。没有重复改造这两项，也不因此宣称所有历史卡片质量合格。

## 验收证据

| 检查 | 实际结果 |
|---|---|
| 升级前应用基线 | 1029 passed、9 skipped |
| 冻结代码应用回归 | **1095 passed、9 skipped、0 failed**，`frozen-regression.log` |
| 独立 Git CI checker | **18 passed**，`ci-checker-tests.log` |
| 生产实际 Python 3.13.14 兼容 | **83 passed、10 subtests passed**，`production-python-compat.log` |
| 外层批处理两行补丁 | **5 passed**，`batch-driver-tests.log`；只用替身，无真实命令 |
| 部署后生产目录回归 | **89 passed、20 subtests passed、0 failed**，`production-tests.log`，Python 3.13.14 |
| 部署后公开入口 | 配置校验、CLI help/status 通过；审核摘要计数对账通过 |
| 项目语法检查 | compileall core/scripts/tests 通过 |
| 发布冻结 | 18 个应用/测试/文档文件 SHA-256 匹配；外层驱动单列清单 |
| 主 CLI 行数门禁 | 1699 / 1700 |

9 项 skip 沿用当前 Windows 符号链接条件。独立 Git checker 与应用回归分开运行，不把跳过当通过。worker 的交付由 Codex 复核；审查发现的跨块过滤和配置校验边界已补测修复。

真实资料只读对照：raw 下相同的 **139 个 Markdown 文件，前后 hash 集合一致，引用坐标错误 0**；零抽取文件 4→1，信息单元 764→807。含 URL 形态的候选也增加，因此不把数量增加当语义准确率提升。详见 `fallback-report.md`。没有真实模型调用或全库卡片重跑。

## 部署边界与复核方式

- 部署清单：`release-files.json`、`external-release.json`；审查差异 `reviewed-release.diff`。
- 执行入口：`deploy_b12.py`。在现有 runner/批处理仍活动时明确拒绝；核对 B11 基线和冻结 hash 后，复用已有定点部署脚本，排除 `.env` 备份。
- 生产 `config.json`、`.env`、`.openclaw`、原始资料及归档卡片保留。知识库所有文件在同步前后逐一核对 SHA-256。
- 外层驱动补丁仅在同一安全窗口替换，保留原脚本备份；当前批次继续使用它已加载的旧代码直到退出。
- 部署后配置、公开 CLI、审核摘要及针对回归已通过；18 个冻结文件 hash 再复核无差异。回退以部署报告中的 backup_dir 对应旧文件为准，不清空任何运行记录或知识页。

## 实际部署与生产验证

生产批处理退出后，活动进程检查通过才开始同步。没有中断该批次，也没有代为应用它留下的已批准计划。

- 定点同步 **18 个应用/测试/文档文件**，另同步外层批处理脚本的两行补丁；既有程序备份 1191 个文件（排除 `.env`）。
- 同步前后 **548 项既有资产 hash 一致**，包括配置、凭据、运行记录与旧验收材料；随后更新本轮交接文档并新增本验收记录，这两份文档是明确交付项。
- 知识库 **2741 个文件**在部署前后及部署后测试后 hash 一致，增加/删除/修改均为 0。
- 生产配置仍为 `initialize.seed_stage=false`。status 快照：2183 份笔记、374 条 processed 来源记录。审核队列 104 项：pending 52、approved 5、rejected 4、applied 43、unknown 0；这些是当时状态，不是自动批准旧队列的依据。
- 本轮未调用真实 provider；故障恢复由受控 HTTP/provider 替身验证，混合批次由隔离真实链路验证。真实服务稳定性、延迟和模型语义质量仍需后续日常运行观察。

证据与回退定位：

- [本轮验收证据目录](D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-22-upgrade)：`production-checks.json`、`production-tests.log`、`acceptance-checks.json`、`vault-integrity.json`、`external-deploy.json`。
- [部署报告](D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-21-live-realgate/evidence/deploy-B12-20260922-2235/deploy-report.json)：verdict 为 PASS，含逐文件 hash 与增改类型。
- 程序备份：`D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-21-live-realgate/evidence/deploy-B12-20260922-2235/backup-20260922-2235`。外层驱动备份在本轮证据目录的 `backup-run_init_batches.py`。
- 如需回退，先停止新任务调度并等活动进程退出，按部署清单还原旧程序文件，处理清单中本轮新增文件；还原外层驱动及旧交接文档。不要整树覆盖当前 `.openclaw`、配置或知识库。

## 后续范围

seed 保持关闭。下一次质量迭代先拿少量单想法、对话和摘录样本做“发芽”样张，验收粒度与真实关联后再决定是否启用；不以卡片数量作成功指标。旧队列清理、历史内容复审、目录重分类、全库重跑和 provider 性能调优留作明确任务。

接手操作说明见发布包内 `docs/B12运行变更.md`。旧外层驱动仍沿用其既有自动批准策略，只能在用户明确允许自动放行的任务中使用；通常运行仍按完整 run_id 逐页审核。
