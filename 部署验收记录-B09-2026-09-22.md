# 部署验收记录 — B09（2026-09-22 17:28）

## 修的是什么

**写作入口（跨源综合）吃不到 `2-领域/`、`3-资源/`、`4-项目/` 里的料。**

`scripts/personal_kb_steward.py:351/425/427` 的 `prepare_writing` 走的是
`core/retrieval.py knowledge_prefixes_with_inputs()`，它把输入目录**写死成 `raw/`**：

```python
def knowledge_prefixes_with_inputs(cfg=None, knowledge=None):
    """Knowledge objects first, `raw/` last: for cross-source synthesis tasks."""
    return (knowledge if knowledge is not None else knowledge_prefixes(cfg)) + ("raw/",)
```

而另一条路 `retrieval_prefixes()` 已经读了 `scan.retrieval_include_dirs`。
→ **生产已开启 B 档，但写作入口完全没吃到，配置层面无解，只能改代码。**

实测后果：写作入口可及性仅 **419/2178 = 19.2%**，主题目录可及 **0 篇**。

## 改了什么

`core/retrieval.py` 一处（+ 测试一处）：

```python
def knowledge_prefixes_with_inputs(cfg=None, knowledge=None):
    """Knowledge objects first, then the recall-scope input dirs: cross-source synthesis."""
    scope = ((cfg or {}).get("scan") or {}).get("retrieval_include_dirs")
    inputs = ("raw/",) if scope is None else tuple(f"{d}/" for d in retrieval_input_dirs(cfg))
    return (knowledge if knowledge is not None else knowledge_prefixes(cfg)) + inputs
```

**设计取舍：未配置时行为完全不变（仍是 `raw/`），只有显式配置才放宽。**
不制造惊喜 —— 老库升级后行为与之前逐字节一致。

⚠️ 再次强调（与 B08 同一条纪律）：这里放宽的是**检索范围**，
`core/layout.py` 的 `INPUT_DIRS`（「知识输出不得与输入目录重叠」的**安全集合**）**没动**。
两者同名同值纯属巧合，绝不可合并。

## 部署文件

| 文件 | 动作 |
|---|---|
| `core/retrieval.py` | updated |
| `tests/test_retrieval.py` | updated（新增 `test_writing_entry_recall_scope_is_configurable_too`） |

## 部署结果（deploy_patch.py B09）

| 项 | 结果 |
|---|---|
| 源哈希 | 2 个文件 |
| 全量备份 | 1148 个文件（**已排除 `.env`**，沿用 B08 治本） |
| 哈希复核 | **2/2 一致** |
| 独有资产 | 507 项 → 变更 **0**、丢失 **0** |
| **verdict** | **PASS** |

## 部署后门禁

| 门禁 | 结果 |
|---|---|
| 定向 `tests/test_retrieval.py` | 18 passed, 1 skipped |
| 写作链路定向（7 个文件） | **91 passed, 1 skipped, 20 subtests** |
| 全量（排除 `test_ci_committed_diff.py`） | **1012 passed, 9 skipped, 89 subtests，0 failed**（117.50s） |
| `git status` 删除行 | **0**（总变更 132，本地长期改动，非本次引入） |
| CLI `--help` | exit 0 |
| `scripts/validate_config.py` | exit 0 |

## 效果（生产真实索引，只读复算）

`iteration-artifacts/2026-09-21-live-realgate/verify_writing_scope_fix.py` → VERDICT PASS

| | 修复前 | 修复后 |
|---|---|---|
| 写作入口可及笔记 | 419/2178 = **19.2%** | 1877/2178 = **86.2%** |
| 其中主题目录（2-领域/3-资源/4-项目） | **0** | **1236** |
| 判据 | — | 只增不减 ✅；主题目录 0 → 正数 ✅ |

## 生产实测：各入口命中（predict_entry_inputs.py）

| 入口 | 修复前 主题目录命中 | 修复后 |
|---|---|---|
| A 沉淀工作记忆 | 1/5 | 4/5（销账后） |
| B 写作素材·AI和媒体 | **0/8** | **5/8** |
| C 选题·AI和媒体 | **0/8** | **7/8** |
| D 写作素材·发现雁荡 | **0/8** | **7/8** |
| E 写作素材·园博园 | **0/8** | **8/8**（全在 `4-项目/园博园`，0 重复） |

挑到的已经是真材实料，例如：
`2-领域/AI和媒体/研究思考/*`、`2-领域/AI和媒体/本单位实践/*`、
`2-领域/发现雁荡/雁荡山概述.md`、`专栏写作计划：《人文雁荡：山海之间的万千气象》`、
`4-项目/园博园/AIGC极限挑战赛/*`。

## 未做 / 待确认

- 本次**未跑 LLM**，未写知识库内容，未改 `.openclaw` 之外的任何东西。
- ④ 凭据副本清理（5 份备份副产品 + `_s0-sandbox/` 2 份）—— **破坏性，待用户确认**。
