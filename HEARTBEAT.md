# HEARTBEAT.md

Keep heartbeats quiet unless explicitly scheduled.

Useful periodic checks:

- Daily: run `mindseed-grow` for fresh `quicknote/` or `raw/` changes.
- Weekly: run `kb-lint-healthcheck` and write `outputs/kb-steward-YYYYMMDD.md`.

