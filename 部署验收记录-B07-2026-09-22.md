# 部署验收记录 — B07 契约字段缺失丢弃候选（2026-09-22）

状态：**已部署，部署后验证通过。**

部署位置：`D:/OLD_VAULT-整理后-20260919/tools/personal-kb-steward`
来源：`.pks-iteration`（迭代区）
时间：2026-09-22 08:06–08:16

## 修的是什么

模型返回内容完整、来源可追溯的候选，只漏了两个**契约元数据**字段 `confidence` / `review_required`，
运行时按「整条 item 不合法」丢弃 → `planned_pages=0`，**而退出码仍是 0**，极易被误读为「跑完了」。

修复：`core/json_contract.py` 新增 `apply_contract_defaults()`，缺失时**默认成最保守值**
（`confidence=low` + 强制 `review_required=True`），保留候选并留下可见的降级提示。
**内容字段（title/summary/sources）缺失仍然报错**，绝不编造内容。

## 同步方式：5 文件定点补丁（不是整树同步）

与 2026-09-21 三处修复那轮**不同**：那次 7 个修复文件中有 4 个在部署副本里不存在，只能整树同步；
本轮被改的 3 个 `.py` 在生产里都已存在，可逐文件替换 + 哈希核验。

| 文件 | 处理 |
|---|---|
| `core/json_contract.py` | 覆盖 |
| `core/skill_runtime.py` | 覆盖 |
| `core/executor_adapters.py` | 覆盖 |
| `LOCAL-PATCHES.md` | 覆盖 |
| `tests/test_contract_defaults.py` | 新增（让部署目录自己能跑定向门禁） |

**未同步**：迭代区独有的 395 个 `docs/iteration-evidence/` 证据文件。
**未触碰**：`config.json`、`.env`、`.openclaw/`、部署侧独有的 6 个文档与 `pr-artifacts/`。
**无删除操作**。

## 验证清单结果

| # | 项目 | 结果 |
|---|---|---|
| 1 | 全量备份 | 1125 个文件 → `evidence/deploy-B07-20260922-0806/backup-20260922-0806` |
| 2 | 5 个文件哈希 = 源哈希 | **5/5 一致** |
| 3 | 部署侧独有资产 486 项 | 变更 0、丢失 0 |
| 4 | 无删除确认（`git status`） | 删除行 **0** |
| 5 | 配置兼容性（新代码 × 旧 `config.json`） | `validate_config.py` 退出 **0**，5 个用户入口齐全，`default mode: dry-run` |
| 6 | CLI 冒烟 `--help` | 正常打印子命令列表 |
| 7 | **部署后定向回归**（覆盖被改模块的 8 个测试文件） | **123 passed in 156.78s** |
| 8 | 部署后全量回归（排除 `test_ci_committed_diff.py`） | **993 passed / 89 subtests / 8 failed in 350.77s** |
| 9 | **相对部署前的新增失败** | **0**（失败集合与部署前逐条相同） |
| 10 | 原始知识库未动 | 今日被修改/新建文件 **0**，新建目录 **0**，`_kb-steward/` 仍 **349** 个文件 |

## 关于那 8 个失败（与本修复无关）

失败集合：

```
tests/test_derived_index.py::test_links_do_not_export_external_notes_or_redirect_cache
tests/test_producer_recovery.py::ProducerRecoveryTests::test_real_init_review_apply_tracks_objects_and_processed_sources
tests/test_producer_recovery.py::ProducerRecoveryTests::test_real_task_review_apply_tracks_objects_and_processed_sources
tests/test_public_baseline_runner_c3.py::test_prepare_vault_rejects_prepopulated_or_escaped_target
tests/test_review_guards.py::ConfigReviewTests::test_symlink_escape_rejected
tests/test_seed_quality_outputs.py::test_init_pause_preserves_plan_then_existing_review_cli_applies_it
tests/test_seed_quality_outputs.py::test_configured_auxiliary_directory_symlink_is_rejected_before_apply
tests/test_windows_write_boundaries.py::test_true_symlink_guard_evidence_or_explicit_unverified
```

均为**符号链接权限类测试**，靠「本机无建符号链接权限」来触发守卫。2026-09-21 基线
（`iteration-artifacts/2026-09-21-deployment/tests.log`：1003 passed / 9 skipped / 0 failed / 181.36s）
里它们是被 **skip** 的 9 项；现在符号链接能建了（Developer Mode / 提权状态变化），
守卫不触发 → `DID NOT RAISE` 断言反转为 failed。

**定性证据**：2026-09-22 把这 8 项**原样拿到未经本修改的部署代码**上复跑 →
**8 failed in 2.05s，清单逐条相同**。**环境漂移，非代码问题。**

## 未闭环的一项

`tests/test_ci_committed_diff.py`（18 项）在这台机器上不稳定：前 17 项通过、最后一项
`test_shallow_clone_does_not_silently_shrink_pr_range` 原地不动。
已在**未改动的部署代码**上复现同样现象（4 分 14 秒无进展后终止）→ 环境问题
（本机单次 git 调用约 1.2–1.6 秒，正常机器约 50ms；该文件每个测试都要建一次性 git 仓库）。

**结论：门禁 #4（全量 `pytest tests/ -q` 必须 0 failed）在本机不可达。**
本轮按修正后的两段式门禁执行：主门禁 = 定向套件（必须 0 failed）；附加门禁 = 全量回归，
判据是「相对基线的新增失败 = 0」并把已知环境失败单独列明。

## 回滚

```
# 按文件还原这 5 项即可
backup-20260922-0806/core/json_contract.py
backup-20260922-0806/core/skill_runtime.py
backup-20260922-0806/core/executor_adapters.py
backup-20260922-0806/LOCAL-PATCHES.md
# tests/test_contract_defaults.py 是本轮新增，回滚即删除
```

**不要用 `/MIR` 整体镜像**——备份是部署前快照，镜像会抹掉部署后新产生的数据。
仅当部署后未产生任何业务数据时才可用 `/MIR`。

## 部署后仍未做的事

- **未在生产库发起任何真实任务**（不调模型、不出 plan、不写库）。
  生产库首次真实 dry-run 需用户明确发起。
- 存量卡片不修复、不全库重跑。
- B06（写作入口）仍未验证（两次被服务端内容过滤拦下）；B02（标题截断）未修。

## 证据目录

- `D:/OLD_VAULT-整理后-20260919/iteration-artifacts/2026-09-21-live-realgate/B07-回归验证结论.md`
- `.../evidence/deploy-B07-20260922-0806/deploy-report.json` — 同步报告（verdict: PASS）
- `.../evidence/deploy-B07-20260922-0806/preserved-before.json` — 部署侧独有资产同步前哈希（486 项）
- `.../evidence/deploy-B07-20260922-0806/post-deploy-gates.log` — 部署后两段门禁
- `.../evidence/B07-verify-staged.log` — 部署前三段式验证
- `.../evidence/deploy-baseline-8tests.log` — 8 个失败项在未改动代码上的复跑（8 failed in 2.05s）
