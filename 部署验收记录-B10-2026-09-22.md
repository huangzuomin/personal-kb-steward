# 部署验收记录 — B10（2026-09-22 19:37）

## 修的是什么

**启发式产出被误判为「不透明机器行」→ `init-kb` 整批抛异常中止。**

复现命令（生产真库）：

```
python scripts/personal_kb_steward.py init-kb --batch-size 3 --no-llm
→ SensitiveContentError: 敏感内容检查阻断：opaque_machine_line
   栈顶 skills/topic-research-compile/executor.py:350 assert_safe_content(data)
```

`assert_safe_content(note)`（原始输入）**先过了**，炸的是 `analyze_note()` 的**产物**
—— 命中串是启发式**构造**出来的，不是原文照抄，所以在 `raw/*.md` 里 grep 是找不到的。

`diagnose_safety_block.py` 逐篇复算：**14 篇 raw 命中**，位置在
`data.topics[0].title` 与 `data.key_facts[N]`。逐条查证后确认**两处都是误报**：

| # | 根因 | 说明 |
|---|---|---|
| 1 | `infer_topics` 回退分支 `re.sub(r"\s+", "", title)` **删掉标题所有空格** | `Amazon Just Killed 50,000 Human Voices` → `AmazonJustKilled50,000HumanVoices`：无空格 + 大小写 + 数字 + 逗号，正好命中 `suspicious_machine_line`。12 篇受影响 |
| 2 | `sentence_chunks` 对 `.`/`!`/`?` **无条件断句** | markdown 图片链接被切碎：`!` 被单独切出后 `[](https://substackcdn.` 不再以 `![` 开头，绕过噪音过滤；残留片段（百分号编码把 `://` 变成 `%3A%2F%2F`，藏掉了豁免用的 `/`）读起来像裸 token |

**不是真凭据。** 故**不改写原文、不换 provider** —— 修的是「启发式不该产出机器样 token」。

## 改了什么（4 个文件）

`core/source_analysis.py`

```python
def _is_noise(sentence: str) -> bool:
    if "\n" in sentence or sentence.startswith(("---", "#", "![", "[![")):
        return True
    if re.match(r"\A[a-zA-Z_]+\s*:", sentence):
        return True
    # 孤立的不透明机器行不是可归因的信息单元（见下方真实故障样本）
    return suspicious_machine_line(sentence)

def infer_topics(...):
    if not topics:
        # 保留单空格：把所有空白折叠掉会把普通标题变成无空格 token
        short = re.sub(r"\s+", " ", title).strip(" -_")[:32].strip() or "资料待整理专题"

# ASCII 句末符只在「后接空白或文末」时断句；CJK 句末符始终断句
_SENTENCE_BOUNDARY = re.compile(r"(?<=[。！？；])\s*|(?<=[.!?])(?=\s|\Z)\s*")
```

`core/content_safety.py` —— 异常携带**规则码**（不携带命中值）：

```python
class SensitiveContentError(ValueError):
    def __init__(self, message: str, reason: str | None = None) -> None:
        super().__init__(message)
        self.reason = reason
```

`skills/topic-research-compile/executor.py` —— **按命中规则类型分流**（3 处）：

```python
# 真凭据 → 安全事件，大声中止整轮；opaque_machine_line 是高误报启发式 → 只拦这一篇
_HARD_SAFETY_REASONS = frozenset({
    "private_key", "credential_token", "bearer_token",
    "credential_assignment", "credential_field",
})
```

⚠️ **这是本轮的关键设计取舍**：`opaque_machine_line` 只记 `blocked` 并 `continue`
（不写页面、不计 processed、在 `input_outcomes` 里可见）；
**真凭据仍然 `raise`**，既有契约测试
`test_secret_in_unused_response_field_is_blocked` 原样通过 —— 安全强度未被削弱。

## 离线复算（先离线，后部署）

`iteration-artifacts/2026-09-21-live-realgate/` 下两个脚本，对**真实 132 篇 raw**：

| 指标 | 修复前（生产） | 修复后 |
|---|---|---|
| 敏感命中（`diagnose_safety_block.py`） | **14 篇** | **0 篇** |
| `bare_token` 形态单元（触发拦截的那类） | 133 | **0** |
| 垃圾形态单元（bare_token + urlish） | 237 | **147**（−38%） |
| 真散文单元（prose） | 524 | **593**（+69） |
| 信息单元合计 | 763 | 740 |

`measure_info_units.py --root <工具目录>` = 跑该根代码；`--diff a.json b.json` 对比。

### 🔴 中途放弃过一个更「大」的修法（有实测依据）

曾一度把**换行也作为切句边界**（可多救回「标题后被粘走的第一句」，prose +13）。
**已撤回**，因为实测发现：

- 换行成边界后，长 JSON/百分号编码碎片会按长度抢进 `extract_info_units` 的 top-6，
  **反而挤掉散文** → 净效果不如不切（prose 只 +13，而最终方案 +69）；
- 更关键：它会让部分输入从「无页面」变成「启发式页面」，而
  **任何非 LLM 源页面在 apply 时都会被 `page_has_blocked_placeholder` 拒绝，
  并 `SystemExit` 中断整轮 apply** → 一个坏页面毒掉整批。

**纪律：不为了一个漂亮的局部指标去改会波及 apply 语义的东西。**

## 部署文件

| 文件 | 动作 |
|---|---|
| `core/source_analysis.py` | updated |
| `core/content_safety.py` | updated |
| `skills/topic-research-compile/executor.py` | updated |
| `tests/test_content_safety_false_positives.py` | **new**（14 个用例） |

## 部署结果（deploy_patch.py B10）

| 项 | 结果 |
|---|---|
| 源哈希 | 4 个文件 |
| 全量备份 | 1173 个文件（**已排除 `.env`**，沿用 B08 治本） |
| 哈希复核 | **4/4 一致** |
| 独有资产 | 532 项 → 变更 **0**、丢失 **0** |
| **verdict** | **PASS** |

## 部署后门禁

| 门禁 | 结果 |
|---|---|
| 生产定向（6 个文件） | **138 passed, 5 subtests** |
| 全量（排除 `test_ci_committed_diff.py`） | **1030 passed, 89 subtests，5 failed**（352s） |
| 那 5 项 failed | **与部署前基线逐项相同**：全是 Windows 符号链接需管理员权限的环境问题，在**未改动的生产代码上同样失败**（对照已跑） |
| `git status` 删除行 | **0** |
| CLI `--help` / `scripts/validate_config.py` | exit 0 |

## 部署后生产实测：**阻塞已解除**

```
python scripts/personal_kb_steward.py init-kb --batch-size 3 --no-llm
→ exit 0；计划落盘 67 页；人工确认队列 22 项
```

修复前同一条命令是 `SensitiveContentError` 整批中止。

## 未做 / 待确认（两条相邻发现，均**预存**、非本次引入）

1. **`page_has_blocked_placeholder` 会因单个非 LLM 页面中断整轮 apply**
   （`scripts/personal_kb_steward.py:1238-1241,1271-1272`）：
   `mode != "llm"` 即判为占位内容 → `SystemExit`。后果：**LLM 批次里只要有一篇
   provider 失败而回退启发式，整轮 apply 会被拒**。这是本轮**没有**动的东西，
   但它会直接影响无人值守批量跑的稳定性，需要单独一轮处理。
2. **换行不是切句边界** → 「标题后的第一句」仍会被整块丢弃（见上文放弃说明）。
   已用 `tests/test_content_safety_false_positives.py::test_heading_prefixed_prose_line_is_dropped_known_limitation`
   把该局限**钉住**，避免它悄悄漂移。

## 未做 / 待确认（沿用）

- ④ 凭据副本清理（5 份备份副产品 + `_s0-sandbox/` 2 份）—— **破坏性，待用户确认**。
- 本轮**未写知识库内容**，未改 `.openclaw` 之外的任何东西。
