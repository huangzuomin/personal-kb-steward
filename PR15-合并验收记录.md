# PR #15 合并后验收记录（本机）

> 目的：把「上游说修好了」变成「我这边逐条查过树」。
> 这份记录只记**实际执行并看到输出**的核验，不转述任何未执行的断言。
> 日期：2026-09-19

## 1. 合并事实

| 项 | 值 | 核验方式 |
| --- | --- | --- |
| PR | #15 MERGED | `gh pr view 15 --json state,mergedAt,mergeCommit` |
| 合并时间 | 2026-09-19T14:22:29Z | 同上 |
| merge commit | `f05e7d1b9eb769e1ba485a55f786f06646e89163` | 同上 |
| 验收 head | `a98506daa981ca9240e0bb474279b8daa29425e0` | `git rev-parse FETCH_HEAD` |
| 远端 HEAD | `f05e7d1`（一致） | `git ls-remote origin HEAD` |

> ⚠️ **`a98506d` 不是 `f05e7d1` 的祖先，这是正常的** —— GitHub 用 squash merge，
> 分支提交不进主线血缘。`merge-base --is-ancestor` 返回 false **不代表没合进去**，
> 查合并状态要用 `gh pr view`。

## 2. 修复是否真落在代码里（`git show f05e7d1:<file>` 逐条查）

| 项 | 结果 |
| --- | --- |
| `renderer.py` 默认目录 | ✅ `"wiki/sources"`（**我曾写成 `_kb-steward/sources`，已由合并者修正**） |
| 非测试代码里的 `_kb-steward` | ✅ 0（`git grep` 只剩 `tests/` 里故意用自定义目录的断言） |
| 无配置默认布局 | ✅ 仍是中性 `wiki/...` |
| `fix_broken_links.py` / `_v2.py` | ✅ 已改为存根：只打印退役说明、`return 1`、**无任何写入调用** |
| 全局路径绑定 | ✅ 已移除；`is_knowledge_path(path, prefixes=("wiki/",))` 改显式参数 |
| 独立入口依赖 | ✅ `reconcile.py` / `synthesize.py` / `kb_index.py` 均不引用知识根，无需先跑主命令 |

## 3. 本机同步

```
git checkout main
git merge --ff-only f05e7d1        → HEAD = f05e7d1 ✅
config.json / .env                  → 未被覆盖（先备份、后 diff 比对，一致）✅
python scripts/validate_config.py   → 「配置校验通过」✅
python scripts/kb_index.py rebuild  → 1890 篇，warnings: [] ✅
```

## 4. 索引篇数差异 —— 查清了，不是丢内容

重建后是 **1890**，旧基线 1926，差 36。核对如下：

```
2188  全库 md
 −136 隔离区（.p0-quarantine）
  −20 归档区（_历史版本 / _重复抓取）
 ────
 2032
   …再排除 模板 / _草稿 / copilot-* / smart-chats / textgenerator / 9-附件 等
 1926  旧基线（未启用 exclude_files）
  −36  0-MOC.md（新 exclude_files 规则）
 ────
 1890  ✅ 精确吻合
```

差的正是 Patch H 生效后排除掉的 **36 个生成型索引页** ——
即「不要把目录页喂给 LLM」这条正在按预期工作。

## 5. 真库完整性

```
全库 md                 2188 ✅
_kb-steward/topics      0 篇 ✅
_kb-steward/sources     0 篇 ✅
_kb-steward/seeds       0 篇 ✅
_kb-steward/concepts    0 篇 ✅
_kb-steward/cases       0 篇 ✅
_kb-steward/material-packs 0 篇 ✅
--apply 对真库          从未执行 ✅
```

## 6. 🔴 我在补丁里犯的错（已由合并者修正）

```python
# ❌ 我写的 —— 把自己库的目录当成「无配置回退值」
sources_dir = str(data.get("sources_dir") or "_kb-steward/sources")
```

任何**没配** `write.sources_dir` 的用户都会被写到 `_kb-steward/`。

**为什么我的验证没抓到**：我做的三方「测试失败集合比对」得出「新增的失败 = 无」，
但**没有任何测试覆盖这个回退值**。

> **失败集合相同 ≠ 没有回归。**
> 行为类回归（默认值、回退常量、配置语义）不在测试覆盖里时，集合比对看不见。

**我的清扫方向也不对**：我只扫了「残留的 `wiki/`」，没扫「注入的 `_kb-steward`」。
**同一个泄漏问题，两个方向都要扫** —— 而且「注入」更容易漏，
因为自己的值看起来是对的、在自己环境跑也正常。

## 7. 剩余边界（上游明确指出，不要越界声称）

- `max_total_source_chars` 只限制**普通 task/plan 的正文合计**，不是所有入口的总请求大小
- 真实 GLM 的产出质量、本机特有的 **5 项失败**，**不能仅凭云端 CI 宣称已解决**
- 不能把结果表述成「带着五个失败照样合并」
- 本次修复与验证**没有操作真实知识库**

## 8. S1 小批量验证：重跑 S0 的 6 篇探针（已完成）

**选这 6 篇的理由**：S0 那两页「温州 AI」假专题正是在这批上冒出来的，
重跑同一批能得到干净的**修复前 / 修复后对照**。

方式：独立沙箱 `_s0-sandbox/`（工具副本已替换为 `f05e7d1` 代码，
保留沙箱自己的 `config.json` 与 `.env`），`init-kb --batch-size 3`，两批跑完 6 篇。

### 结果

| | 修复前（S0） | 修复后（S1 / f05e7d1） |
| --- | --- | --- |
| 6 篇探针产出 | **8 页** = 6 source + **2 假专题** | **6 页** = 6 source，**0 假专题** ✅ |
| 专题 / 聚合页 | 温州人工智能创新发展路径<br>温州AI政策与产业研究资料包 | **0** ✅ |
| 每页 sources | 两页都挂**全部 6 篇** | 每页只挂**自己那一篇** ✅ |
| confidence / review | medium / **false** | high / false（source 页无需复核，合理）✅ |
| 产出含 demo 语料 | 有 | **0 命中** ✅ |

### 内容抽检（01 雁荡山 —— S0 里被误挂到「温州 AI」的那篇）

- 摘要准确：陶冷月、1937 年 8 月赴雁荡山拍摄 51 幅、《取景神妙：陶冷月民国写真》收录 48 幅
- **提取的 3 个专题全部来自内容本身**：
  陶冷月与「新中国画」中西融合／陶冷月摄影实践与民国画意摄影／1937 雁荡山历史影像
  —— **没有凭空造专题**
- 质量标记**主动标注不确定项**：文末被图片链接截断、自媒体标题党、
  48 与 51 幅数字出入、依赖《陶冷月年谱长编》等二手转引
  —— **LLM 不硬编结论，会自陈证据边界**

> 这与 S0「LLM 面对不匹配素材会主动说不」的观察一致，
> 是「LLM 行为可靠」的第二次独立佐证。

### 两个执行层面的注意（不是工具缺陷）

1. **`init-kb --apply` 在本次没有直接落盘** —— 日志显示它确实调用了 apply-plan，
   但 apply-plan 返回 1。根因是**我这边宿主环境的 `[safe-delete]` 钩子**拦了临时文件清理
   （`count:53 > threshold:50`，目标是工具写的 `.write-check-*.tmp`）。
   ⇒ **环境噪音，不要当成上游缺陷报。** 手动 `apply-plan <plan>` 正常成功。
2. `init-kb` 仍无法指定篇目，只能按索引顺序取 —— 见 `HANDOVER.md §7.2`。
   本次用「先 apply 第一批，让 processed index 前进，第二批自然取到 04–06」绕过。

## 9. 下一步

可在**真库**上按 batch-size 3 分批推进 `init-kb`（配置默认 dry-run，安全）。
真库目前仍是 0 知识页，`--apply` 从未对真库执行过。
不再套临时补丁。
