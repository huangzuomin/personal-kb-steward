# 依赖复查尚未生成

这是一份占位说明，不代表有零项待复查内容。

在已配置的 Steward 仓库运行 `python scripts/workspace_report.py`，读取最新快照。
该命令只向标准输出打印 Markdown，不覆盖本文件，不修改知识页和审核队列。

需要索引时，先显式运行 `python scripts/kb_index.py rebuild`。索引是缓存，不能把重建当成证据已经更新。
样例库附有预先生成的快照；打开时间与其中检查时间可能不同。
